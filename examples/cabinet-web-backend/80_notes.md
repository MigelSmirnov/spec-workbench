# State 7 — Cabinet Web Backend generation notes

## Browser and source transport

derive_browser_csrf_token: [DETERMINISM_OR_ORDERING] MUST return the lowercase hexadecimal HMAC-SHA256 keyed by the UTF-8 bytes of credential_pepper over the UTF-8 bytes of the compact sorted-key ensure_ascii-False json.dumps of the object with exactly the keys handoff_id and principal_id; equal inputs always derive the same value, and it MUST NOT use random material, delimiter concatenation, str, repr, reflection, or object identity.
issue_browser_csrf: [RETURN_SHAPE] MUST return BrowserCsrfToken(handoff_id=handoff_id, csrf_token=derive_browser_csrf_token(gateway.settings.credential_pepper, owner.principal.principal_id, handoff_id)) for the owner the route resolved from the TrustedEdge assertion; it opens no UoW, stores nothing, reads no handoff, and never returns a value for an absent owner.
accept_source_upload: [SECURITY_BOUNDARY] MUST take the owner already resolved by the route from the TrustedEdge assertion — it performs no authentication call of its own and never treats a CSRF value, a header, or a form field as a credential — require request.csrf_token to equal derive_browser_csrf_token(gateway.settings.credential_pepper, owner.principal.principal_id, request.handoff_id) under a timing-safe comparison (a mismatch raises AuthorizationDeniedError and consumes nothing), then call store_original_source with request.handoff_id, request.handoff_secret, request.display_filename, request.declared_media_type and the still-bounded BinaryUpload stream; never treat a filename, media declaration, handoff identifier, or browser control as storage-path or authorization evidence.
issue_browser_csrf_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
accept_source_upload_handler: [ORCHESTRATION] MUST read the TrustedEdge owner assertion the private nginx hop injects — exactly one X-Cabinet-Authenticated-Owner header value of two space-separated parts whose scheme is TrustedEdge (case-insensitive) and whose second part is the non-empty assertion; any other count or shape raises AuthenticationRequiredError — resolve the owner through resolve_browser_principal on the retained access control with that assertion, and never import from api (api registers this handler), enforce same-origin (the Origin header equals the request scheme and Host) and require the X-CSRF-Token header to equal the csrf_token form field before consumption, declare exactly the two parameters handoff_secret and csrf_token as multipart form fields — each defaulted to Form(...) imported from fastapi — while handoff_id stays the bare path parameter and file stays the UploadFile — never as query or header parameters, so the secret never enters a URL — derive one safe display filename from the multipart UploadFile filename, construct BrowserUploadRequest with that filename and file.content_type, call accept_source_upload with the gateway, that owner, that request and one WebGatewayUploadFileBinaryUpload adapter over the multipart UploadFile and gateway.settings.upload_max_file_bytes whose synchronous read_chunk(max_bytes) reads at most max_bytes from file.file and enforces gateway.settings.upload_max_file_bytes across the complete stream without pre-buffering or interpreting the document; it reads no cookie, MUST NOT call UploadFile.read, buffer the complete upload, or instantiate the BinaryUpload interface itself.
issue_upload_handoff: [BEHAVIOR] MUST require authorization.allowed, authorization.capability == the capability of the single imported CAPABILITY_GRANTS row whose operation field is 'issue_upload_handoff' — never the effect operation kind and authorization.principal_id == command.actor.principal_id, treat an absent authorization.entity_scope as unscoped authority and a present one as valid only when its entity_id == command.card_id (otherwise ValidationRejectedError with InvoiceRejectionCode.ACCESS_DENIED from = models.InvoiceRejectionCode), then inside one transaction lock the Card, require its current reference content_hash == command.expected_revision.content_hash, load that revision through load_card_revision(command.card_id, its content_hash), parse InvoiceCardV1.model_validate_json(revision.canonical_json) and require its source.source_id == command.source_id (the logical source is the Card's own source reference), lock the custody pair and, when load_source_custody returns None, insert SourceCustodyRecord(card_id=command.card_id, source_id=command.source_id, content=None, custody_status= the imported PENDING_CUSTODY_STATUS constant, accepted_at=None, rejected_at=None, released_at=None, rejection_reason=None, actor=command.actor) before issuing a new single-use handoff whose status is UploadHandoffStatus.ISSUED from = models.UploadHandoffStatus and whose expires_at is issued_at plus custody.settings.upload_handoff_ttl_seconds; persist only the verifier and return the bearer value once without placing it in Card data or logs; constructs the one-time SourceUploadHandoff value at the protected source-custody boundary.
issue_upload_handoff: [ORCHESTRATION] MUST open one CabinetUnitOfWork through unit_of_work_factory.open, begin, perform every Card and revision read and the handoff insert inside that transaction, commit exactly once on success, and roll back on every refusal or unexpected exception.
store_original_source: [PROVENANCE] MUST lock the exact handoff with `lock_handoff`, read bytes only through upload.read_chunk, identify the content format as the unique format among the imported CONTENT_FORMAT_SIGNATURES rows whose every row segment (offset, bytes_hex) matches the stored bytes, reject bytes matching no entry, and require declared_media_type to be absent or equal to the identified entry's media_type, preserve display_filename only as display/provenance metadata, stream to private candidate storage while calculating size and content hash, require the locked handoff to be usable: its status == UploadHandoffStatus.ISSUED from = models.UploadHandoffStatus, consumed_at and revoked_at both None, and the retained clock's now() strictly before handoff.expires_at — any other state raises ValidationRejectedError with InvoiceRejectionCode.ACCESS_DENIED from = models.InvoiceRejectionCode, consuming and storing nothing; atomically commit immutable custody and consume the handoff by setting status to UploadHandoffStatus.CONSUMED from = models.UploadHandoffStatus and consumed_at to that same now() inside the one open transaction, and reject bytes that conflict with already-stored custody content; neither display_filename nor declared_media_type is storage-path or authorization evidence.
store_original_source: [ORCHESTRATION] MUST open one CabinetUnitOfWork through unit_of_work_factory.open and begin before calling lock_handoff, keep every custody read, insert and handoff update inside that transaction, commit exactly once after the bytes are published, and roll back on every refusal or unexpected exception.
retrieve_original_source: [SECURITY_BOUNDARY] MUST resolve only the authorized Card/source pair to its immutable verified content reference, reopen a private regular file beneath the configured store, recheck integrity, and return one CanonicalSourceFileDownload over its exact ContentReference and the retained SourceByteStore after verifying that file, whose describe() returns the exact ContentReference of the verified custody content with content_id=request.source_id, with no filesystem path or credential; the module constructs CanonicalSourceFileDownload; MUST NOT instantiate the SourceDownload interface itself.
retrieve_original_source: [ORCHESTRATION] MUST open one CabinetUnitOfWork through unit_of_work_factory.open, begin, call load_source_custody for request.card_id and request.source_id inside that transaction, and finally call `rollback`; this read path never commits.
serve_source_download: [PATH_OR_ARTIFACT_POLICY] MUST construct SourceRetrievalRequest(card_id=card_id, source_id=source_id, authorization=authorization) — every declared field, the authorization included — and return the concrete SourceDownload implementation obtained from source custody through retrieve_original_source with that request and the same authorization as a non-inline attachment source using an encoded safe display filename and verified media type, disable active-content interpretation, and never expose the internal storage reference in headers, body, logs, or errors; MUST NOT instantiate the SourceDownload interface itself.

## Authentication, authorization, and application composition

derive_authentication_abuse_context: [DETERMINISM_OR_ORDERING] MUST return canonical compact sorted-key JSON with channel and the closed known-or-unknown classification, adding credential_id only when the supplied credential is non-null and exactly matches both channel and credential_id; otherwise it returns the unknown classification without attacker-chosen identity material. It MUST NOT include secret material, accept caller context, or create separate unknown buckets.
derive_entity_scope_key: [DETERMINISM_OR_ORDERING] MUST return the lowercase hexadecimal SHA-256 over the UTF-8 bytes of the imported ENTITY_SCOPE_DOMAIN_TAG constant immediately followed by the compact sorted-key ensure_ascii-False json.dumps of one payload object whose single key scope is null for the unscoped form and, for the scoped form, an object with exactly the keys entity_id, entity_kind and revision, revision being null when entity_scope.revision is None and otherwise the JSON-mode model dump of that CardRevisionReference after observed_at is normalized to timezone-aware UTC (a naive value taken as UTC). It MUST NOT invent another prefix or separator, use delimiter concatenation, omit revision constituents, inline a computed JSON template, or use str, repr, reflection, or object identity.
issue_security_audit_id: [PROVENANCE] MUST return a newly generated UUID4 string on every call. Every SecurityAuditRecord construction MUST call it once and MUST NOT reuse a credential, principal, node, grant, request, or timestamp identity as `evidence_id`.
require_authenticated_local_node: [RETURN_SHAPE] MUST return principal.node only when principal.channel is CabinetChannel.LOCAL_NODE from = models.CabinetChannel, principal.principal.principal_kind equals PrincipalKind.LOCAL_BACKEND_NODE from = models.PrincipalKind, node is non-null and active with node_kind CabinetNodeKind.LOCAL_BACKEND from = models.CabinetNodeKind, and node.principal_id equals principal.principal.principal_id; otherwise it raises AuthorizationDeniedError. It performs no I/O, reflection, fallback, or mutation and never constructs or substitutes another node.
authenticate_request: [ORCHESTRATION] MUST return exactly authenticate_channel_request(service.unit_of_work_factory, service.credential_pepper, service.clock, service.settings, channel, presented_credential). It opens no UoW, catches no delegated exception, and contains no credential, throttle, subject-resolution, audit, or fallback implementation.
authorize_capability: [ORCHESTRATION] MUST return exactly evaluate_capability_grant(service.unit_of_work_factory, service.clock, principal, capability, entity_scope). It opens no UoW, catches no delegated exception, and contains no grant lookup, scope-key, audit, or fallback implementation.
enroll_principal: [ORCHESTRATION] MUST return exactly bootstrap_or_enroll_principal(service.unit_of_work_factory, service.credential_pepper, service.clock, operator, command). It opens no UoW, catches no delegated exception, and never treats command data as authentication proof.
enroll_local_node: [ORCHESTRATION] MUST return exactly enroll_local_backend_node(service.unit_of_work_factory, service.credential_pepper, service.clock, operator, command). It opens no UoW, catches no delegated exception, and never treats command data as authentication proof.
provision_capability_grant: [ORCHESTRATION] MUST return exactly persist_capability_grant(service.unit_of_work_factory, service.clock, grantor, command). It opens no UoW, catches no delegated exception, and contains no private grant store or fallback implementation.
rotate_credential: [ORCHESTRATION] MUST return exactly rotate_principal_credential(service.unit_of_work_factory, service.credential_pepper, service.clock, operator, command). It opens no UoW, catches no delegated exception, and never retains either bearer.
revoke_credential: [ORCHESTRATION] MUST return exactly revoke_principal_credential(service.unit_of_work_factory, service.clock, operator, command). It opens no UoW, catches no delegated exception, and never treats command data as authentication proof.
authenticate_channel_request: [SECURITY_BOUNDARY] MUST open one UoW through unit_of_work_factory.open and begin. It parses the self-identifying bearer, loads only the candidate credential identity when present, supplies that candidate to derive_authentication_abuse_context only when its channel exactly matches, hashes the derived context through derive_abuse_context_hash, and acquires the locked throttle state. Active throttle records fresh denied audit, commits, and raises AuthenticationThrottledError. Otherwise it verifies through verify_bearer_credential and resolves exactly one subject: subject_kind equal to CredentialSubjectKind.PRINCIPAL from = models.CredentialSubjectKind loads an active CabinetPrincipal whose principal_kind is one of {PrincipalKind.CABINET_OWNER, PrincipalKind.OPERATOR} from = models.PrincipalKind and produces AuthenticatedPrincipal with node None; subject_kind equal to CredentialSubjectKind.NODE from = models.CredentialSubjectKind loads an active CabinetNodeIdentity with node_kind CabinetNodeKind.LOCAL_BACKEND from = models.CabinetNodeKind, checks its contract_version equals the imported NODE_CONTRACT_VERSION constant, loads by node.principal_id the exact active CabinetPrincipal whose principal_kind equals PrincipalKind.LOCAL_BACKEND_NODE from = models.PrincipalKind, and produces AuthenticatedPrincipal with that node. After the subject branches it MUST test the resolved principal once: a principal that is still None — any other subject_kind included — takes the failure path below before any AuthenticatedPrincipal is constructed. Every malformed, unknown, mismatched-kind/channel, inactive, revoked, unbound, cross-installation, or incompatible case takes one indistinguishable failure path: register_authentication_failure with the supplied settings, fresh denied audit, commit, AuthenticationRequiredError. Success updates last_authenticated_at, registers success, appends fresh allowed audit, commits, and returns AuthenticatedPrincipal; plaintext is never retained. Every unexpected exception rolls back before re-raise.
resolve_trusted_browser_owner: [SECURITY_BOUNDARY] MUST accept only exact TrustedEdge label `owner`, open one UoW through unit_of_work_factory.open, begin, call `list_principals_by_kind` with PrincipalKind.CABINET_OWNER from = models.PrincipalKind, finally call `rollback` after the read, and return AuthenticatedPrincipal with node None only when exactly one active non-revoked principal of that kind exists; every missing, duplicate, inactive, revoked, other-label, or unexpected case rolls back and raises AuthenticationRequiredError. It never validates Basic Auth itself or accepts a public listener.
evaluate_capability_grant: [BEHAVIOR] MUST open one operation UoW through unit_of_work_factory.open, begin, re-read the exact principal, and for channel local_node additionally re-read principal.node and require that node is active, node.principal_id equals principal.principal.principal_id, and node.contract_version equals the imported NODE_CONTRACT_VERSION constant; other channels require node None. It derives the scope key only through derive_entity_scope_key and loads the exact principal/channel/capability/scope grant. It re-evaluates lifecycle and the imported CAPABILITY_GRANTS rows, appends audit with fresh issue_security_audit_id, commits and returns only the matching scoped decision; every denial or unexpected exception rolls back before re-raise.
persist_capability_grant: [SECURITY_BOUNDARY] MUST open one UoW through unit_of_work_factory.open, begin, re-read the separately supplied AuthenticatedPrincipal grantor, require its principal active with principal_kind in {PrincipalKind.CABINET_OWNER, PrincipalKind.OPERATOR} from = models.PrincipalKind, require an active target, require at least one imported CAPABILITY_GRANTS row whose channel equals command.channel and capability equals command.capability — a row's operation field names the semantic operation and never participates in grant matching — derive the scope key only through derive_entity_scope_key, and load the exact existing grant before conditionally inserting one CapabilityGrantRecord plus fresh SecurityAuditRecord. Exact replay commits no duplicate and returns created false; every refusal or unexpected exception rolls back without changing grant state.
bootstrap_or_enroll_principal: [PROVENANCE] MUST open and begin one UoW through unit_of_work_factory.open. Null operator is accepted only when no CabinetPrincipal with principal_kind in {PrincipalKind.CABINET_OWNER, PrincipalKind.OPERATOR} from = models.PrincipalKind exists and command.principal_kind equals PrincipalKind.CABINET_OWNER from = models.PrincipalKind; otherwise it re-reads the separately supplied AuthenticatedPrincipal operator and requires its principal active with principal_kind in {PrincipalKind.CABINET_OWNER, PrincipalKind.OPERATOR} from = models.PrincipalKind. A command.principal_kind equal to PrincipalKind.LOCAL_BACKEND_NODE from = models.PrincipalKind is refused with AuthorizationDeniedError before any write, because node enrollment belongs to enroll_local_backend_node. It inserts one CabinetPrincipal, mints its channel-bound credential on the same UoW with subject_kind CredentialSubjectKind.PRINCIPAL from = models.CredentialSubjectKind and subject_id equal to the new principal_id, appends fresh SecurityAuditRecord, commits, and returns the bearer once; every refusal or unexpected exception rolls back, exposes no bearer, and persists no partial row.
enroll_local_backend_node: [SECURITY_BOUNDARY] MUST open and begin one UoW through unit_of_work_factory.open, re-read the separately supplied AuthenticatedPrincipal operator through load_principal and require its principal active with principal_kind in {PrincipalKind.CABINET_OWNER, PrincipalKind.OPERATOR} from = models.PrincipalKind, require load_node for command.node_id to return None, read now from the retained clock exactly once, insert one CabinetPrincipal whose principal_id is a fresh issue_security_audit_id value with principal_kind PrincipalKind.LOCAL_BACKEND_NODE from = models.PrincipalKind and status active, insert through insert_node one CabinetNodeIdentity with node_id = command.node_id, principal_id = that new principal_id, node_kind CabinetNodeKind.LOCAL_BACKEND from = models.CabinetNodeKind, status active, contract_version = the imported NODE_CONTRACT_VERSION constant, created_at = now and revoked_at None, mint the credential on the same UoW through mint_credential with subject_kind CredentialSubjectKind.NODE from = models.CredentialSubjectKind, subject_id = command.node_id, channel CabinetChannel.LOCAL_NODE from = models.CabinetChannel, rotated_from_credential_id None and minted_at = now, append one fresh SecurityAuditRecord for the enrollment, commit, and return IssuedNodeCredential carrying the inserted node and principal, the minted credential_id, its channel, the bearer and its issued_at exactly once. Every refusal raises AuthorizationDeniedError; every refusal or unexpected exception rolls back, exposes no bearer, and persists no partial row. It never re-binds, updates or revokes an existing node and never mints a principal-subject credential.
rotate_principal_credential: [SECURITY_BOUNDARY] MUST open one UoW through unit_of_work_factory.open, begin, re-read the separately supplied AuthenticatedPrincipal operator and require its principal active with principal_kind in {PrincipalKind.CABINET_OWNER, PrincipalKind.OPERATOR} from = models.PrincipalKind, lock and load the named credential, verify exact principal/channel binding, retire it and mint the replacement on the same UoW with rotated_from_credential_id and the retired record's exact subject_kind, subject_id and channel, append fresh SecurityAuditRecord, and commit atomically. It preserves subject identity/channel, returns the new bearer once, never recovers the old bearer, and rolls back every refusal or unexpected exception.
revoke_principal_credential: [BEHAVIOR] MUST open one UoW through unit_of_work_factory.open, begin, re-read the separately supplied AuthenticatedPrincipal operator and require its principal active with principal_kind in {PrincipalKind.CABINET_OWNER, PrincipalKind.OPERATOR} from = models.PrincipalKind, lock and load only the named credential, verify exact principal binding, retire only that record, append fresh SecurityAuditRecord, and commit. Exact repeated revocation is idempotent; every mismatch, unauthorized request, or unexpected exception rolls back.
credential_vault: [SECURITY_BOUNDARY] MUST keep every plaintext secret transient: no function persists, logs, or retains a presented or minted bearer beyond its return value. No function begins, commits, or rolls back a transaction: the caller owns the supplied CabinetUnitOfWork. Across the module, every persisted, emitted, or compared wall-clock timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, and only a naive datetime is a defect. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
parse_bearer_credential: [BEHAVIOR] MUST split the presented value on the first `.` into `<credential_id>.<secret_material>` and return the pair only when both parts are non-empty; every other shape returns None without raising.
derive_credential_verifier: [DETERMINISM_OR_ORDERING] MUST return the lowercase hex sha256 digest of the UTF-8 bytes of `credential_pepper + "|" + bearer` and nothing else; the same pepper and bearer always derive the same verifier.
mint_credential: [PROVENANCE] MUST generate a collision-resistant credential identity and cryptographically random secret material, assemble the self-identifying bearer `<credential_id>.<secret_material>`, derive its verifier through derive_credential_verifier, insert one active channel-bound AccessCredentialRecord carrying only that verifier through `insert_credential` on the supplied UoW with issued_at = minted_at and rotated_from_credential_id recorded verbatim, and return the record together with the bearer exactly once; the plaintext bearer is never persisted.
verify_bearer_credential: [SECURITY_BOUNDARY] MUST parse the presented value through parse_bearer_credential, call `load_credential` for the parsed identity on the supplied UoW, derive the presented verifier through derive_credential_verifier, and return the record only when it exists, its channel equals the expected channel, its status is active, revoked_at is None, and the verifier comparison uses hmac.compare_digest; every failure path returns None after indistinguishable handling and a malformed bearer never raises.
retire_credential: [BEHAVIOR] MUST return the credential unchanged when it is already revoked, and otherwise update it to status revoked with revoked_at = retired_at through `update_credential` on the supplied UoW and return the updated record; repeated retirement of the same credential is idempotent.
abuse_throttle: [BEHAVIOR] MUST treat AuthenticationThrottleState as the only persisted truth: every transition is written through `upsert_throttle_state` on the supplied CabinetUnitOfWork and no function begins, commits, or rolls back a transaction. Across the module, every persisted, emitted, or compared wall-clock timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, and only a naive datetime is a defect. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
derive_abuse_context_hash: [DETERMINISM_OR_ORDERING] MUST return the lowercase hex sha256 digest of the UTF-8 bytes of `credential_pepper + "|" + abuse_context`; the same pepper and context always derive the same hash.
acquire_throttle_state: [ORCHESTRATION] MUST call `lock_abuse_context` for the exact hash and then `load_throttle_state` on the supplied UoW, and return the loaded state or, when absent, a fresh AuthenticationThrottleState with the supplied hash, zero consecutive_failures, no delay, no block, no credential, and updated_at = observed_at; it MUST NOT persist the fresh state itself.
throttle_active: [BEHAVIOR] MUST return true exactly when blocked_until or delay_until is set and strictly later than observed_at; it reads only the supplied state and performs no I/O.
register_authentication_failure: [BEHAVIOR] MUST derive the new consecutive_failures as the prior count plus one and write the state through `upsert_throttle_state` with credential_id as supplied, updated_at = observed_at, blocked_until = observed_at plus settings.auth_throttle_seconds seconds when the new count is at least settings.auth_failures_before_throttle and otherwise None, and delay_until = None; thresholds come only from the declared configuration and the updated state is returned.
register_authentication_success: [BEHAVIOR] MUST write the state through `upsert_throttle_state` with consecutive_failures = 0, delay_until = None, blocked_until = None, credential_id as supplied, and updated_at = observed_at, and return the updated state.
extract_plugin_credential: [SECURITY_BOUNDARY] MUST accept only one well-formed Bearer value on the private ChatGPT route namespace, reject missing, duplicated, oversized, or wrong-scheme headers uniformly, and never log or echo the value.
extract_local_node_credential: [SECURITY_BOUNDARY] MUST accept only one well-formed Bearer value on the synchronization route namespace, reject missing, duplicated, oversized, or wrong-scheme headers uniformly, and keep it distinct from the ChatGPT credential scope.
extract_browser_principal: [SECURITY_BOUNDARY] MUST accept only the TrustedEdge owner assertion injected by the private nginx hop after Basic Auth; reject direct or duplicated assertions, and rely on deployment enforcement that strips the client-supplied header before injection.
resolve_plugin_principal: [SECURITY_BOUNDARY] MUST delegate the single ChatGPT Bearer to authenticate_request with CabinetChannel.PLUGIN from = models.CabinetChannel, let that operation finish its owned transaction, and accept only when the returned active non-revoked principal has principal_kind equal to PrincipalKind.CABINET_OWNER from = models.PrincipalKind. It MUST NOT open another UoW or wrap the transaction-owning authentication call; it MUST reject machine, browser, revoked, absent, or ambiguous identities without an anonymous fallback and raise AuthenticationRequiredError for every failed resolution.
resolve_browser_principal: [ORCHESTRATION] MUST return exactly resolve_trusted_browser_owner(access_control.unit_of_work_factory, access_control.clock, principal_assertion). It opens no UoW, catches no delegated exception, and contains no Basic Auth, principal query, or fallback implementation.
resolve_local_node_principal: [RETURN_SHAPE] MUST return one complete AuthenticatedPrincipal with its exact bound CabinetNodeIdentity for the single accepted synchronization credential and never return an owner, anonymous, raw node, or partial identity.
resolve_capability: [DETERMINISM_OR_ORDERING] MUST allow only when one imported CAPABILITY_GRANTS row has capability equal to the requested dotted value and channel equal to the requested channel, returning that row's operation as the single semantic operation in catalogue order, and MUST refuse any requested capability that appears in the imported PROTECTED_OPERATOR_CAPABILITIES rows for every principal — those rows carry only capability and operation and have no channel field, and reject unknown, affix-confused, or cross-channel names without dynamic dispatch.
create_app: [DEPENDENCY_BOUNDARY] MUST preserve the application composition boundary and require all thirteen explicit module services, bind them to private application state, install only the accepted ChatGPT, browser, and synchronization routers plus empty-body exception mappings, and fail startup when protected configuration or any required service is unavailable.

## ChatGPT proposal and effect semantics

prepare_chatgpt_proposal: [BEHAVIOR] MUST treat conversational fields as untrusted suggestions, normalize them into the requested typed proposal whose normalized_request is the canonical JSON string (sorted keys, compact separators) of an object holding the parsed request.suggested_content JSON object, the resolved capability string and request.target_revision.model_dump(mode='json') when present, preserve missing and uncertain facts, attach exact revision and duplicate warnings, and perform no durable Card, source, effect, or synchronization mutation.
confirm_chatgpt_effect: [SECURITY_BOUNDARY] MUST verify that the owner explicitly confirmed the exact proposal digest, target revision, capability, and effect identity before expiry; any mismatch, ambiguity, decline, or stale revision produces no authorization and no mutation.
report_composite_outcome: [RETURN_SHAPE] MUST return separate typed Card, original-source custody, and local-synchronization outcomes with bounded safe codes, preserving partial or unknown states instead of collapsing them into a single success flag.
begin_effect: [BEHAVIOR] MUST reserve one principal-scoped idempotency identity before mutation using only unit_of_work.lock_effect, unit_of_work.load_effect_by_idempotency, unit_of_work.load_effect and unit_of_work.insert_effect inside the caller's transaction, return the prior compatible reservation or committed result on replay, reject conflicting payload reuse, and preserve EffectStatus.OUTCOME_UNKNOWN from = models.EffectStatus for read-only reconciliation; a new reservation is inserted with effect_id equal to request.idempotency_key — the one identity every caller later presents to commit_effect — with status EffectStatus.PREPARED from = models.EffectStatus, created_at=the retained clock's now() and completed_at=None.
commit_effect: [PROVENANCE] MUST bind the exact durable operation result and completion time to the existing effect reservation through unit_of_work.lock_effect, unit_of_work.load_effect and unit_of_work.update_effect_result inside the caller's transaction, so the bound result commits together with the domain write; reject absent, conflicting, or already-differently-committed effects and never rewrite prior evidence.
reconcile_effect: [BEHAVIOR] MUST read the exact reserved effect and its durable result evidence through unit_of_work.load_effect for effect_id, require effect.principal_id == principal_id, without issuing the mutation again, returning committed, rejected, or outcome_unknown truthfully and leaving all domain state unchanged.

## Canonical Card and Invoice operations

commit_card_revision: [ORCHESTRATION] MUST open and begin one fresh CabinetUnitOfWork, delegate all revision preparation and writes to commit_card_revision_in_unit_of_work, commit only its successful result, and call `rollback` on every exception; it MUST NOT duplicate Card canonicalization or expose a committed result before the UoW commits.
prepare_card_revision: [PROVENANCE] MUST parse the supplied canonical Card JSON as a generic JSON object, require the parsed card_type to equal command.card_type and the parsed id to equal command.card_id, and accept every declared card_type this way; it MUST NOT restrict card_type to an enumerated subset or parse through per-type models. MUST reject every error-severity validation issue, compute content_hash as the imported canonical_json_text_digest(command.canonical_json) — the imported REVISION_HASH_ALGORITHM constant names that procedure in the stored revision and is never passed to hashlib —, read observed_at from the retained clock's now() exactly once per operation, use that same value for CardRevisionReference.observed_at and CanonicalCardRevision.created_at, set the reference observed_status to the imported REVISION_OBSERVED_STATUS constant, and return the complete CanonicalCardRevision carrying that canonical serialization without reading or mutating persistence.
commit_card_revision_in_unit_of_work: [ORCHESTRATION] MUST require an already active caller-owned CardRevisionCommitUnitOfWork, call lock_card for command.card_id, read the stored current selector through load_current_card_reference and compare its content hash with the command expected_content_hash, call prepare_card_revision, append the immutable revision through insert_card_revision, and insert or move the current selector through insert_current_card_reference or update_current_card_reference; it MUST NOT begin, commit, roll back, create another UoW, or reach the unit of work through reflective attribute access.
commit_card_revision_in_unit_of_work: [RETURN_SHAPE] MUST return the exact stored revision with created true and replayed false after a new append, return created false and replayed true only for an identical already-current revision, and otherwise raise the owned validation, not-found, canonical-hash, or revision-conflict error without changing persistence.
search_provider_cards: [DETERMINISM_OR_ORDERING] MUST open, begin, and finally call `rollback` on one CabinetUnitOfWork, call list_current_card_references_by_type for the imported PROVIDER_CARD_TYPE constant, load every referenced revision through load_card_revision and parse ProviderCard from its canonical_json, keep a card only when every present criterion matches — query.text as a case-insensitive substring of the title or a notes entry, query.service equal to a services entry, query.area equal to a service_areas entry, query.language equal to a languages entry value — treating external text as data and never as a pattern, order matches by Provider Card id, resume strictly after the id equal to query.cursor when a cursor is present, include at most query.limit items, and set next_cursor to the last included id when further ordered matches remain and None otherwise; it performs no mutation.
list_project_cards: [DETERMINISM_OR_ORDERING] MUST open, begin, and finally call `rollback` on one CabinetUnitOfWork, call list_current_card_references_by_type for the imported PROJECT_CARD_TYPE constant, load every referenced revision through load_card_revision and parse ProjectCard from its canonical_json, construct each ProjectSummary from the parsed ProjectCard, its exact revision reference and the card's own accepted-estimate, shopping-list and invoice-link facts, keep a card only when query.status and query.client_id, where present, match by exact equality, order by card_id, resume strictly after the card_id equal to query.cursor when a cursor is present, include at most query.limit items, and set next_cursor to the last included card_id when further ordered references remain and None otherwise; it never substitutes Registry snapshots for Project Card master facts.
get_card_revision: [BEHAVIOR] MUST open, begin, and finally call `rollback` on one CabinetUnitOfWork, call load_card_revision with reference.card_id and reference.content_hash, raise ResourceNotFoundError when no such revision is stored or when the stored reference card_type differs from reference.card_type, and return the stored CanonicalCardRevision unchanged; it never silently substitutes the current or newer revision.
load_invoice_revisions: [ORCHESTRATION] MUST call list_current_card_references_by_type on the supplied open unit_of_work with the imported INVOICE_CARD_TYPE constant, call load_card_revision for every returned card_id and content_hash, parse each revision through parse_invoice_revision, and never open, commit, or roll back the unit of work itself.
load_invoice_revisions: [RETURN_SHAPE] MUST return one (reference, parsed Card) pair per returned reference in the exact order the unit of work returned them, with no reference skipped, substituted by its current head, or synthesized.
parse_invoice_revision: [ORCHESTRATION] MUST call InvoiceCardV1.model_validate_json on revision.canonical_json and require the parsed card_type and card_version to equal the imported INVOICE_CARD_TYPE constant and the imported INVOICE_CARD_VERSION constant.
parse_invoice_revision: [VALIDATION_ERROR] MUST raise ValidationRejectedError with InvoiceRejectionCode.NOT_FOUND from = models.InvoiceRejectionCode for malformed JSON or a card type/version mismatch, and MUST NOT repair, default, or partially construct the Card.
require_mutation_authorization: [SECURITY_BOUNDARY] MUST require authorization.allowed, authorization.capability == capability with that capability present in the imported CAPABILITY_GRANTS rows, and authorization.principal_id == actor.principal_id. An absent authorization.entity_scope carries unscoped capability authority and passes for any target; when entity_scope is present it MUST identify invoice_id, with revision equal to expected_revision when expected_revision is not None and with no revision otherwise. Any failure raises ValidationRejectedError with InvoiceRejectionCode.ACCESS_DENIED from = models.InvoiceRejectionCode before any read or write.
require_mutation_authorization: [RETURN_SHAPE] MUST return None on success and MUST NOT mutate, log secrets, or fabricate an authorization decision.
load_expected_draft: [ORCHESTRATION] MUST call the imported get_invoice_in_unit_of_work function with the retained catalogue as its first argument, then the active operation unit_of_work, invoice_id and expected_revision.content_hash, require the returned reference to equal expected_revision and the parsed invoice id to equal invoice_id, and raise ValidationRejectedError with InvoiceRejectionCode.REVISION from = models.InvoiceRejectionCode on any mismatch or InvoiceRejectionCode.NOT_FOUND from = models.InvoiceRejectionCode when the revision is absent.
load_expected_draft: [RETURN_SHAPE] MUST return the exact InvoiceCardView from the catalogue without substituting the current head revision.
commit_invoice_successor: [ORCHESTRATION] MUST call validate_invoice through the retained validator with InvoiceValidationInput(invoice=successor, expected_revision=None), reject any issue at the imported VALIDATION_ISSUE_SEVERITY constant with InvoiceRejectionCode.VALIDATION from = models.InvoiceRejectionCode, construct CardRevisionCommitCommand(card_id=successor.id, card_type=the imported INVOICE_CARD_TYPE constant, expected_content_hash=expected_content_hash, canonical_json=successor.model_dump_json(), validation_issues=validation.issues, actor=actor), and call commit_card_revision_in_unit_of_work through the retained card_workspace with the supplied unit_of_work exactly once.
commit_invoice_successor: [RETURN_SHAPE] MUST construct InvoiceMutationResult(effect_id=effect_id, revision=commit_result.revision, status=commit_result.revision.observed_status, replayed=commit_result.replayed, issues=validation.issues, safe_error_code=None); a commit failure propagates the card_workspace rejection and never returns expected_content_hash as new evidence.
search_invoices: [ORCHESTRATION] MUST open, begin, and finally call `rollback` on one CabinetUnitOfWork; call list_current_card_references_by_type with the imported INVOICE_CARD_TYPE constant, load and parse every returned reference through load_invoice_revisions, and separately index list_available_working_set_records for the imported WORKING_SET_AVAILABLE_STATUS constant by revision before building views; it MUST NOT call a mutation.
search_invoices: [DETERMINISM_OR_ORDERING] MUST filter parsed Cards by exact query.status, inclusive issue_date bounds, supplier.tax_id for query.provider_id, and casefolded substring query.text over invoice_number and the supplier name, tax_id and address; order by issue_date descending with missing last, then reference.card_id and reference.content_hash, resume strictly after the item whose content_hash equals query.cursor, and emit at most query.limit items.
search_invoices: [RETURN_SHAPE] MUST construct each InvoiceCardView with the loaded reference, parsed Card, source_status = invoice.source.file_status, and synchronization_status = the matching working-set status or the imported SYNCHRONIZATION_UNAVAILABLE_STATUS constant; next_cursor is the final emitted content_hash only when more filtered items remain, otherwise None.
get_invoice: [ORCHESTRATION] MUST open, begin, and finally call `rollback` on one CabinetUnitOfWork and return the exact result of get_invoice_in_unit_of_work with that unit of work, invoice_id and content_hash; it MUST NOT read Card state through any other path.
get_invoice: [RETURN_SHAPE] MUST return the exact InvoiceCardView produced by get_invoice_in_unit_of_work without substituting the current head revision.
get_invoice_in_unit_of_work: [ORCHESTRATION] MUST read inside the caller's already active unit_of_work: use load_current_card_reference(invoice_id) only when content_hash is None, then load_card_revision with the resolved card_id and content_hash, take the returned revision's own reference as the reference it reports (a CardRevisionReference is never synthesized from the arguments), parse it through parse_invoice_revision, require the reference card_type and the parsed id to match the imported INVOICE_CARD_TYPE constant and invoice_id, and never substitute another revision; it MUST NOT open, begin, commit, or roll back the unit of work.
get_invoice_in_unit_of_work: [RETURN_SHAPE] MUST construct InvoiceCardView from the exact stored reference and parsed Card, set source_status from invoice.source.file_status, and derive synchronization_status only from a list_available_working_set_records result whose revision equals the reference, falling back to the imported SYNCHRONIZATION_UNAVAILABLE_STATUS constant; any missing or malformed record raises ValidationRejectedError with InvoiceRejectionCode.NOT_FOUND from = models.InvoiceRejectionCode.
find_invoice_duplicates: [ORCHESTRATION] MUST open, begin, and finally call `rollback` on one CabinetUnitOfWork; call list_current_card_references_by_type with the imported INVOICE_CARD_TYPE constant, load and parse each returned revision through load_invoice_revisions, and read candidate values only through the imported DUPLICATE_CANDIDATE_FIELD_SOURCES constant; no stored revision may be synthesized or replaced by its current head.
find_invoice_duplicates: [BEHAVIOR] MUST compare the candidate with every loaded revision on the fields named by the imported DUPLICATE_CANDIDATE_FIELD_ORDER, reading each field of the candidate and of the revision card at the path the imported DUPLICATE_CANDIDATE_FIELD_SOURCES maps it to: strings compare after strip plus casefold, dates and Decimal values compare exactly, a field whose candidate value is None is not comparable, and a revision whose card id equals candidate.invoice_id is excluded (the imported DUPLICATE_INVOICE_ID_MATCH_REQUIRES_EXACT_IDENTITY names that exclusion); MUST construct one InvoiceDuplicateCandidate per matching revision with invoice_revision = its stored reference and reason_codes = the imported DUPLICATE_REASON_CODE_PREFIX followed by each matched field name, in the order of DUPLICATE_CANDIDATE_FIELD_ORDER.
find_invoice_duplicates: [DETERMINISM_OR_ORDERING] MUST return the empty tuple when fewer comparable fields exist than the imported DUPLICATE_MINIMUM_MATCH_FIELDS or when no revision matches at least that many fields; otherwise MUST sort the constructed candidates by the attribute paths named by the imported DUPLICATE_STABLE_ORDER, applied in order to each candidate, and never merge, archive, confirm, or select a winner.
prepare_invoice_draft: [ORCHESTRATION] MUST call InvoiceCardV1.model_validate_json on proposal.canonical_json, require the parsed card_type to equal the imported INVOICE_CARD_TYPE constant and card_version to equal the imported INVOICE_CARD_VERSION constant, require proposal.invoice_id when present to equal invoice.id, proposal.currency to equal invoice.currency, and invoice.source.source_id to be non-empty; then call validate_invoice exactly once and reuse its duplicate_candidates.
prepare_invoice_draft: [RETURN_SHAPE] MUST construct InvoiceDraftProposal(invoice=validation.normalized_invoice, issues=validation.issues, duplicate_candidates=validation.duplicate_candidates); JSON/model mismatch raises ValidationRejectedError with InvoiceRejectionCode.VALIDATION from = models.InvoiceRejectionCode and no field, identity, actor, source, total, or payment is invented.
validate_invoice: [ORCHESTRATION] MUST obtain the ordered issue tuple through evaluate_validation_checks for candidate.invoice; then construct InvoiceDuplicateCandidateInput from invoice id, invoice_number, supplier.tax_id, issue_date and totals.gross as gross_total and call find_invoice_duplicates through the retained catalogue exactly once.
validate_invoice: [RETURN_SHAPE] MUST construct InvoiceValidationResult(normalized_invoice=candidate.invoice, issues=the complete tuple sorted by the imported VALIDATION_ISSUE_ORDER constant and line index, duplicate_candidates=the exact duplicate result, revision_context=candidate.expected_revision); it performs no write and never suppresses a failed declared check.
create_invoice_draft: [SECURITY_BOUNDARY] MUST before any write call require_mutation_authorization with command.actor, authorization, the operation capability, command.draft.invoice.id and no expected revision; failure raises ValidationRejectedError with InvoiceRejectionCode.ACCESS_DENIED from = models.InvoiceRejectionCode.
create_invoice_draft: [ORCHESTRATION] MUST require command.draft.invoice.status InvoiceLifecycleState.DRAFT from = models.InvoiceLifecycleState, no issue at the imported VALIDATION_ISSUE_SEVERITY constant, and no duplicate_candidates; construct CardRevisionCommitCommand(card_id=invoice.id, card_type=the imported INVOICE_CARD_TYPE constant, expected_content_hash=None, canonical_json=invoice.model_dump_json(), validation_issues=draft.issues, actor=command.actor) and call commit_card_revision_in_unit_of_work with the operation's unit_of_work exactly once.
create_invoice_draft: [RETURN_SHAPE] MUST construct InvoiceMutationResult(effect_id=command.effect_id, revision=commit_result.revision, status=commit_result.revision.observed_status, replayed=commit_result.replayed, issues=command.draft.issues, safe_error_code=None); rejection raises the declared validation code and never returns command input as stored evidence.
update_invoice_draft: [SECURITY_BOUNDARY] MUST call require_mutation_authorization with command.actor, authorization, the operation capability, command.invoice_id and command.expected_revision; mismatch raises InvoiceRejectionCode.ACCESS_DENIED from = models.InvoiceRejectionCode before any write.
update_invoice_draft: [ORCHESTRATION] MUST load the current revision through load_expected_draft with the active operation unit_of_work for command.invoice_id and command.expected_revision, require its status InvoiceLifecycleState.DRAFT from = models.InvoiceLifecycleState, require command.proposal.invoice.id equal command.invoice_id and no error-severity issues or duplicates, then commit proposal.invoice through commit_invoice_successor with the operation's unit_of_work, expected_content_hash=command.expected_revision.content_hash, command.actor and command.effect_id.
update_invoice_draft: [RETURN_SHAPE] MUST construct InvoiceMutationResult from command.effect_id, commit_result.revision, that revision observed_status, commit_result.replayed, proposal.issues and safe_error_code=None; not-found/stale/invalid input raises the declared revision or validation rejection and never returns command.expected_revision as a new result.
confirm_invoice: [SECURITY_BOUNDARY] MUST call require_mutation_authorization with command.actor, authorization, the operation capability, command.invoice_id and command.expected_revision, read operation_utc_time from the retained clock's now() exactly once per operation, and require confirmation.effect_id == command.effect_id, confirmation.capability == authorization.capability, confirmation.principal_id == authorization.principal_id, confirmation.target_revision == command.expected_revision, and confirmation.expires_at later than operation_utc_time; failure raises InvoiceRejectionCode.CONFIRMATION from = models.InvoiceRejectionCode before mutation.
confirm_invoice: [ORCHESTRATION] MUST open and begin one unit of work through unit_of_work_factory.open — the lifecycle receives it as the narrow InvoiceLifecycleUnitOfWork port — call `lock_card` for command.invoice_id, require load_current_card_reference(command.invoice_id) == command.expected_revision, load and parse that exact draft, validate it through validate_invoice on the retained validator so that every row of the imported INVOICE_VALIDATION_CHECKS table passes with no error, then apply the line-capture check before any confirmation effect: command.line_capture_complete must be True, command.source_line_count a positive integer equal to len(invoice.lines), and every line an Invoice Line of the source — otherwise ValidationRejectedError with InvoiceRejectionCode.LINE_CAPTURE from = models.InvoiceRejectionCode and nothing is written; a warning acknowledgement never bypasses this check, derive confirmed_invoice via model_copy changing only status to InvoiceLifecycleState.CONFIRMED from = models.InvoiceLifecycleState and provenance to invoice.provenance.model_copy(update={confirmed_at: the retained clock now()}), and call commit_card_revision_in_unit_of_work with the same UoW, exact expected hash, confirmed_invoice.model_dump_json(), validation issues and command.actor; it MUST insert through insert_line_capture_evidence one InvoiceLineCaptureEvidence(invoice_id=invoice.id, source_id=invoice.source.source_id, source_line_count=command.source_line_count, captured_line_count=len(invoice.lines), line_capture_complete=True, card_content_hash=the committed revision content_hash, state=InvoiceLifecycleState.CONFIRMED from = models.InvoiceLifecycleState, recorded_at=the retained clock now(), operation=the operation name confirm, previous_card_content_hash=command.expected_revision.content_hash, revision_reason=None, revised_at=None) bound to the exact committed hash, then commit once, while every exception calls `rollback` on the shared UoW so Card, capture proof and effect remain atomic.
confirm_invoice: [RETURN_SHAPE] MUST after commit return InvoiceMutationResult(effect_id=command.effect_id, revision=commit_result.revision, status=commit_result.revision.observed_status, replayed=commit_result.replayed, issues=validation issues, safe_error_code=None). Confirmation creates no transfer manifest or working set under A20 and cannot substitute for canonical admission; it returns no uncommitted revision.
record_invoice_payment: [SECURITY_BOUNDARY] MUST call require_mutation_authorization with command.actor, authorization, the operation capability, command.invoice_id and command.expected_revision before any read-modify-write.
record_invoice_payment: [ORCHESTRATION] MUST load the current revision through load_expected_draft with the active operation unit_of_work for command.invoice_id and command.expected_revision, parse command.payment_json with InvoiceCardPayment.model_validate_json, derive successor = invoice.model_copy(update={payment: parsed_payment}), then commit the successor through commit_invoice_successor with the operation's unit_of_work, the expected content hash, command.actor and command.effect_id; it MUST NOT construct CardRevisionCommitCommand or call commit_card_revision itself; no transfer state implies payment.
record_invoice_payment: [RETURN_SHAPE] MUST construct InvoiceMutationResult from command.effect_id, commit_result.revision and replayed, revision.observed_status, validation issues and safe_error_code=None; parsing, balance, authorization, or revision failure raises the matching the members of = models.InvoiceRejectionCode value and returns no expected revision as new evidence.
attach_invoice_source_metadata: [SECURITY_BOUNDARY] MUST call require_mutation_authorization with command.actor, authorization, the operation capability, command.invoice_id and command.expected_revision before any read-modify-write.
attach_invoice_source_metadata: [ORCHESTRATION] MUST load the current revision through load_expected_draft with the active operation unit_of_work for command.invoice_id and command.expected_revision, require command.source.source_id == invoice.source.source_id (source identity is never minted or replaced here) and command.source.kind, file_status to be members of InvoiceSourceKind and InvoiceSourceFileStatus from = models.InvoiceSourceKind, derive successor by replacing only invoice.source with command.source, then commit the successor through commit_invoice_successor with the operation's unit_of_work, the expected content hash, command.actor and command.effect_id; it MUST NOT construct CardRevisionCommitCommand or call commit_card_revision itself; file_ref is an opaque source reference — never a storage path or logical identity — and custody state stays in the SourceCustodyRecord kept by source_id.
attach_invoice_source_metadata: [RETURN_SHAPE] MUST construct InvoiceMutationResult from command.effect_id, commit_result.revision and replayed, revision.observed_status, validation issues and safe_error_code=None; source, revision, validation, or commit failure raises the declared rejection and never fabricates custody success.
archive_invoice: [SECURITY_BOUNDARY] MUST call require_mutation_authorization with command.actor, authorization, the operation capability, command.invoice_id and command.expected_revision, read operation_utc_time from the retained clock's now() exactly once per operation, and require command.confirmation.effect_id == command.effect_id, command.confirmation.capability == authorization.capability, command.confirmation.principal_id == authorization.principal_id, command.confirmation.target_revision == command.expected_revision, and command.confirmation.expires_at later than operation_utc_time; failure raises InvoiceRejectionCode.CONFIRMATION from = models.InvoiceRejectionCode before mutation.
archive_invoice: [ORCHESTRATION] MUST load the current revision through load_expected_draft with the active operation unit_of_work for command.invoice_id and command.expected_revision, reject an already archived Card, derive successor by changing only status to InvoiceLifecycleState.ARCHIVED from = models.InvoiceLifecycleState, and commit the successor through commit_invoice_successor with the operation's unit_of_work, the exact expected hash, command.actor and command.effect_id; it never deletes earlier revisions, sources, receipts, working sets, or release evidence.
archive_invoice: [RETURN_SHAPE] MUST construct InvoiceMutationResult from command.effect_id, commit_result.revision and replayed, revision.observed_status, validation issues and safe_error_code=None; confirmation, stale, invalid, or commit failure raises the matching declared rejection and never reports command.expected_revision as newly committed.

## Project artifacts

get_project_summary: [BEHAVIOR] MUST derive the summary from the exact Project Card revision, accepted estimate, shopping-list snapshots, and explicit ProjectInvoiceLinks, marking gaps and unmatched facts without mutating any source artifact; an absent project raises ResourceNotFoundError before any derivation — gap_codes mark missing derived artifacts of an existing revision and never substitute for the Project Card.
get_project_summary: [ORCHESTRATION] MUST, when content_hash is None, open one CabinetUnitOfWork through unit_of_work_factory.open, begin, call load_current_card_reference for project_id inside that transaction, and finally call `rollback`; when content_hash is not None the reference handed to get_card_revision is that current reference with only content_hash replaced through model_copy, card_type and every other field kept — a CardRevisionReference is never freshly constructed here; this read path never commits, and every Card revision, estimate and shopping-list read goes through the retained card_workspace operations.
validate_estimate: [RETURN_SHAPE] MUST return the normalized estimate proposal, deterministic net, tax, and gross totals, exact Project revision context, and structured issues; invalid or missing monetary facts remain visible and are never defaulted into acceptance.
derive_shopping_list: [DETERMINISM_OR_ORDERING] MUST from one pinned accepted estimate, derive stable item identities and ordered planned quantities and totals, preserve estimate provenance, and return a proposal without saving or merging it.
attach_project_estimate: [PROVENANCE] MUST require the exact Project revision and authorized reviewed estimate, append the immutable AcceptedEstimateSnapshot and successor Project revision atomically, and reject stale or conflicting attachment without replacing earlier snapshots.
save_shopping_list: [PROVENANCE] MUST preserve the immutable artifact effect boundary and require a reviewed derivation pinned to one Project and estimate revision, issue a new immutable ShoppingListSnapshot version with stable ordering, and never overwrite or silently merge a prior issued list.

## Invoice exchange and Registry replica

observe_sync_compatibility: [SECURITY_BOUNDARY] MUST record a bounded observation for the already authenticated local node supplied as node — it performs no authentication call, opens no UoW, and never treats request.node_id as a credential — before any package issue: authenticated is True; available is True exactly when node.status is active and node.revoked_at is None; compatible is True exactly when available, request.node_id equals node.node_id, and request.contract_version equals both node.contract_version and the imported NODE_CONTRACT_VERSION constant; local_node_id is node.node_id, remote_contract_version is the imported NODE_CONTRACT_VERSION constant, and safe_error_code is None when compatible and otherwise a bounded code naming the failed condition; it returns incompatible or unavailable truthfully without probing or calling into the local network.
discover_invoice_work: [DETERMINISM_OR_ORDERING] MUST snapshot only available Invoice work into one bounded InvoiceWorkPage whose membership is one InvoiceWorkingSetMembership ordered by Invoice ID and manifest ID, include exact Card and source hashes plus an opaque cursor, and never export Provider, Client, Project, estimate, or shopping-list data.
discover_invoice_work: [ORCHESTRATION] MUST open, begin, and finally call `rollback` on one CabinetUnitOfWork, call list_available_working_set_records with the imported WORKING_SET_AVAILABLE_STATUS constant, map every record unchanged to InvoiceWorkingSetItem from its invoice_id, revision, manifest_id, manifest_hash and required_sources, order items by invoice_id then manifest_id, resume strictly after the item whose manifest_id equals query.cursor when a cursor is present, and include at most query.limit items; it performs no write.
discover_invoice_work: [RETURN_SHAPE] MUST read observed_at from the retained clock's now() exactly once per operation and return InvoiceWorkPage whose membership carries vps_node_id=node.node_id, that observed_at value, the ordered bounded work_items, working_set_id equal to the working-set id of the first included item or the empty string for an empty page, and continuation_cursor equal to the page next_cursor, where next_cursor is the last included item's manifest_id when further ordered records remain and None otherwise.
pull_invoice_package: [ORCHESTRATION] MUST open and begin one UoW, resolve_pull_working_set for the exact manifest, resolve its ready admission through read_manifest_admission, and read the exact retained Card envelope through read_admitted_card_revision before recording issuance. For each manifest member in order, use retrieve_source_set_file with the admission Card, the logical source ID from its set custody, exact set hash and selected content hash, passing AuthorizationDecision with allowed=True, principal_id=node.principal_id, capability=the imported CANONICAL_PACKAGE_READ_CAPABILITY, entity_scope=EntityScope(entity_kind=admission.revision.card_type, entity_id=manifest.invoice_id, revision=admission.revision), decided_at from the retained clock and reason_code=None. This strictly narrows the authenticated active node’s already accepted sync-pull authority to its admitted package; it never grants generic download authority or accepts an unverified caller node. Verify each stream description equals the manifest ContentReference. Load any exact assignment observation without inferring one, call issue_transfer_package in the same transaction, commit once, then stream the unchanged length-prefixed InvoicePackageMetadata and original bytes in manifest order. Every pre-commit failure rolls back and issues nothing; no load_card_revision from the legacy product repository is used for canonical work.
pull_invoice_package: [SECURITY_BOUNDARY] MUST stream only working-set sources verified by resolve_pull_working_set, never expose a storage reference or filesystem path on the wire, and classify any post-issuance interruption as EffectStatus.OUTCOME_UNKNOWN from = models.EffectStatus without automatic reissue; MUST NOT instantiate the InvoicePackageStream interface itself.
resolve_pull_working_set: [ORCHESTRATION] MUST call load_transfer_manifest for the exact manifest_id, derive the working-set id from the imported WORKING_SET_ID_PREFIX constant and the manifest_id suffix after the imported MANIFEST_ID_PREFIX constant, call load_working_set_record, require working set status the imported WORKING_SET_AVAILABLE_STATUS constant, matching manifest_id, equal manifest_hash on manifest and working set, and exactly the imported MANIFEST_CARD_REVISION_COUNT constant card revision in the manifest; absence of either record raises ValidationRejectedError with InvoiceRejectionCode.NOT_FOUND from = models.InvoiceRejectionCode before issuance.
resolve_pull_working_set: [SECURITY_BOUNDARY] MUST resolve the exact READY admission, require ordered working required_sources equal to the manifest file identities/hash/size/media projection and match the admitted source set, load the exact set custody, obtain its retained observation through get_registered_source_observation and call inspect_source_set_custody in the same transaction to verify every required file. No singleton load_source_custody or logical Card source ID can stand in for multi-file membership. A mismatch or unavailable member rejects before issuance, without substituting the first photo or a newer revision.
issue_transfer_package: [ORCHESTRATION] MUST derive issuance_id from the imported ISSUANCE_ID_PREFIX constant plus the imported string_tuple_digest((manifest.manifest_id, node.node_id, command.idempotency_key)), call lock_transfer_issuance then load_transfer_issuance_by_idempotency inside the supplied active UoW, and return an existing issuance for the same manifest, node and idempotency key unchanged as replay; otherwise read issued_at from the retained clock's now() exactly once per operation, construct InvoiceTransferIssuance with that issuance_id, the manifest identity and hash, manifest invoice_id, node.node_id, command.idempotency_key, status TransferIssuanceStatus.ISSUED from = models.TransferIssuanceStatus, that issued_at value and no acknowledgement evidence, and call insert_transfer_issuance exactly once.
issue_transfer_package: [RETURN_SHAPE] MUST return the persisted or replayed InvoiceTransferIssuance itself and never a reconstructed value that differs from stored evidence.
InvoiceExchangeInvoicePackageStream.__init__: [DEPENDENCY_BOUNDARY] MUST retain metadata_bytes (the length-prefixed InvoicePackageMetadata JSON exactly as framed by the caller) and the supplied sources exactly in order — one verified SourceDownload per manifest source reference in source_references order — and MUST NOT read, buffer, hash, re-frame or reopen any of them at construction.
InvoiceExchangeInvoicePackageStream.read_chunk: [BEHAVIOR] MUST return at most max_bytes bytes taken in order — the retained metadata_bytes first, then each retained SourceDownload by calling its read_chunk(max_bytes) until it returns empty bytes before advancing to the next — and MUST return b"" only when the metadata bytes and every source are exhausted; it never re-opens a part, regenerates metadata or exposes a storage reference.
open_verified_source: [SECURITY_BOUNDARY] MUST open one operation UoW, call load_source_custody for reference.card_id and reference.source_id, require custody_status the imported STORED_CUSTODY_STATUS constant and stored content equal by value to the supplied SourceContentReference, load the verified bytes through the retained store's read_stored with final_reference_for(reference.content_hash), reference.content_hash and reference.size_bytes, and discard those verification bytes and return one CanonicalSourceFileDownload over the exact ContentReference and retained SourceByteStore, whose describe() returns the exact ContentReference of the verified custody content with content_id=reference.source_id, with no filesystem path or credential; absence or mismatch raises ValidationRejectedError with InvoiceRejectionCode.SOURCE_CUSTODY from = models.InvoiceRejectionCode; MUST NOT instantiate the SourceDownload interface itself, MUST NOT be exposed as a transport operation, and MUST NOT read bytes from module or service state.
open_verified_source: [ORCHESTRATION] MUST open the operation UoW through unit_of_work_factory.open, begin before calling load_source_custody, and finally call `rollback`; this read path never commits.
record_invoice_transfer_receipt: [PROVENANCE] MUST accept only the authenticated issuance node, lock the issuance with `lock_transfer_issuance` and load it with `load_transfer_issuance`, require an exact receipt whose synchronization, Invoice, manifest hash, and accepted hashes match the persisted issuance, append it once through `insert_transfer_receipt`, update the issuance through `update_transfer_issuance` to TransferIssuanceStatus.ACKNOWLEDGED from = models.TransferIssuanceStatus with the receipt evidence, and reject wrong-node, malformed, conflicting, or partial acknowledgement.
reconcile_invoice_transfer: [BEHAVIOR] MUST open, begin, and finally call `rollback` on one CabinetUnitOfWork; read the exact node-scoped issuance with `load_transfer_issuance`, the matching receipt with `load_transfer_receipt`, and the conflict state with `load_synchronization_conflict`, together with local durable-verification evidence, returning exactly one the members of = models.ReconciliationStatus value without issuing another package or choosing a revision winner.
publish_registry_catalogue: [DETERMINISM_OR_ORDERING] MUST verify cabinet-web-sync-v1; that delivery.source_node_id equals the authenticated node id — admission already guarantees the local backend node kind and no other kind is re-derived — and delivery.target_node_id equals the self node label; idempotency, declared count, project_id ordering, monotonic observation, and the canonical content digest before atomically committing the complete replica and current selector; partial or older delivery leaves current unchanged. The canonical content digest is the imported catalogue_content_digest(delivery) — projects stay in delivery order, no field is renamed, added or omitted, and no hand-built JSON template stands in for the model; a delivery whose content_hash differs from that digest is rejected before any write. Every refusal — digest mismatch, node mismatch, contract version, declared count, ordering, older observation, conflicting idempotency reuse — returns VpsCatalogueAcknowledgement with status CatalogueAcknowledgementStatus.REJECTED from = models.CatalogueAcknowledgementStatus and a non-null safe_error_code and raises nothing; the first atomic commit returns status CatalogueAcknowledgementStatus.ACCEPTED from = models.CatalogueAcknowledgementStatus and an exact replay returns the recorded acknowledgement with status CatalogueAcknowledgementStatus.ALREADY_ACCEPTED from = models.CatalogueAcknowledgementStatus.
publish_registry_catalogue: [ORCHESTRATION] MUST open one CabinetUnitOfWork through unit_of_work_factory.open, begin it, and inside that single transaction call lock_registry_current for the self node label — the CabinetNodeKind.LOCAL_BACKEND value from = models.CabinetNodeKind, the one label the read path resolves — resolve replay through load_registry_publication_by_idempotency for delivery.source_node_id and delivery.idempotency_key — an identical committed delivery returns the recorded acknowledgement without mutation and conflicting reuse is rejected — record the publication through insert_registry_publication or update_registry_publication, persist the accepted catalogue as insert_registry_snapshot for a catalogue identity not yet stored, insert_registry_replica or update_registry_replica for the replica row keyed by that same self node label, and insert_registry_current_selector or update_registry_current_selector for the current selection keyed by that same self node label — never by the delivering node id, then commit exactly once while every exception calls `rollback`; a stored snapshot is immutable and MUST NOT be updated or re-inserted, and equal catalogue identity with different content is rejected.
get_current_registry_catalogue: [RETURN_SHAPE] MUST return the last complete accepted Registry replica, exact catalogue identity and hash, ordered project snapshots, freshness and last-publication evidence; stale or offline is explicit and never rewrites Project Cards.
get_current_registry_catalogue: [ORCHESTRATION] MUST open one CabinetUnitOfWork through unit_of_work_factory.open, begin, call load_registry_current_selector with the self node label — the CabinetNodeKind.LOCAL_BACKEND value from = models.CabinetNodeKind — and, when a selector exists, load_registry_replica and load_registry_snapshot for the selected catalogue inside that same transaction, and finally call `rollback`; this read path never commits or mutates replica state.

## VPS release and runtime recovery

release_vps_working_set: [SECURITY_BOUNDARY] MUST require exact owner confirmation plus accepted or already-accepted receipt for the manifest, exact Card and all source hashes, accepted durable verification with evidence ID, and equality of required, verified, and manifest source IDs before releasing only eligible bytes and preserving all logical evidence.
release_vps_working_set: [ORCHESTRATION] MUST open one CabinetUnitOfWork through unit_of_work_factory.open, begin, perform every receipt, custody and publication read and the release write inside that transaction, commit exactly once on success, and roll back on every refusal or unexpected exception.
evaluate_readiness: [BEHAVIOR] MUST report ready only when protected configuration, durable state proven by opening, beginning and rolling back one CabinetUnitOfWork, credential verifiers, private listener assumptions, contract compatibility, and backup evidence are available; the retained dependencies are present by construction and are never probed for attributes or methods at runtime; expose bounded component status without secrets.
verify_backup_restore: [ORCHESTRATION] MUST select one protected backup, restore it into an isolated disposable location, verify exact Card revisions, source hashes and relationships, effects, transfer, Registry and release evidence, record the drill result, and never replace live state with the test restore. Transaction shape under A17: open one unit of work and begin exactly once; inside that single active transaction read the backup evidence, insert the BackupRestoreDrillRecord for this drill_id with started_at, then update the same record with the final status, verified counts, completed_at and safe_error_code, and commit exactly once; a failed verification is a recorded outcome committed in that same transaction and reported through the returned report, while an unexpected exception rolls back once in finally and propagates; no unit-of-work operation is called after commit or rollback and no second transaction is begun for the same drill.

## Deterministic Router IR handlers

search_provider_cards_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
list_project_cards_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
search_invoices_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
get_invoice_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
find_invoice_duplicates_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
prepare_invoice_draft_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
validate_invoice_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
create_invoice_draft_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
update_invoice_draft_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
confirm_invoice_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
record_invoice_payment_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
attach_invoice_source_metadata_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
archive_invoice_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
get_project_summary_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
validate_estimate_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
derive_shopping_list_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
attach_project_estimate_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
save_shopping_list_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
issue_upload_handoff_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
release_vps_working_set_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
serve_source_download_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
observe_sync_compatibility_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.
discover_invoice_work_handler: [ORCHESTRATION] MUST resolve one local-node AuthenticatedPrincipal, authorize with that complete AuthenticatedPrincipal, require its node is non-null, and pass only `authenticated.node` to discover_invoice_work; it preserves the remaining closed request/response/error projection from http_router_backend/v1 without reflection or dynamic dispatch.
pull_invoice_package_handler: [ORCHESTRATION] MUST resolve one local-node AuthenticatedPrincipal, authorize with that complete AuthenticatedPrincipal, require its node is non-null, and pass only `authenticated.node` to pull_invoice_package; it preserves the remaining closed request/stream/error projection from http_router_backend/v1 without reflection or dynamic dispatch.
record_invoice_transfer_receipt_handler: [ORCHESTRATION] MUST resolve one local-node AuthenticatedPrincipal, authorize with that complete AuthenticatedPrincipal, require its node is non-null, and pass only `authenticated.node` to record_invoice_transfer_receipt; it preserves the remaining closed request/response/error projection from http_router_backend/v1 without reflection or dynamic dispatch.
reconcile_invoice_transfer_handler: [ORCHESTRATION] MUST resolve one local-node AuthenticatedPrincipal, authorize with that complete AuthenticatedPrincipal, require its node is non-null, and pass only `authenticated.node` to reconcile_invoice_transfer; it preserves the remaining closed request/response/error projection from http_router_backend/v1 without reflection or dynamic dispatch.
publish_registry_catalogue_handler: [ORCHESTRATION] MUST resolve one local-node AuthenticatedPrincipal, authorize with that complete AuthenticatedPrincipal, require its node is non-null, and pass only `authenticated.node` to publish_registry_catalogue; it preserves the remaining closed request/response/error projection from http_router_backend/v1 without reflection or dynamic dispatch.
get_current_registry_catalogue_handler: [ORCHESTRATION] MUST preserve the closed effect boundary, use only the authentication, dependency wiring, canonical operation, request projection, response projection, and error mapping fixed for this handler by http_router_backend/v1, and return exactly that closed response shape without adding business policy or dynamic dispatch.


## Strict semantic closure

commit_card_revision: [BEHAVIOR] MUST commit exactly one validated successor or return an explicit stale or conflict failure without a partial mutation.
save_shopping_list: [BEHAVIOR] MUST issue exactly one immutable snapshot or return an explicit stale or validation failure without a partial mutation.
commit_effect: [BEHAVIOR] MUST commit exactly one matching reserved effect result or return an explicit conflict without rewriting prior evidence.
create_app: [ORCHESTRATION] MUST construct and return exactly one FastAPI application from the thirteen required services or fail startup before serving requests.
resolve_plugin_principal: [RETURN_SHAPE] MUST return one AuthenticatedPrincipal for the single accepted ChatGPT credential and never return an anonymous or partial principal.
resolve_browser_principal: [RETURN_SHAPE] MUST return one AuthenticatedPrincipal for the single accepted trusted-edge assertion and never return an anonymous or partial principal.
resolve_local_node_principal: [SECURITY_BOUNDARY] MUST delegate the single synchronization Bearer to authenticate_request with CabinetChannel.LOCAL_NODE from = models.CabinetChannel, open no second UoW, and return only an AuthenticatedPrincipal whose principal is active with principal_kind PrincipalKind.LOCAL_BACKEND_NODE from = models.PrincipalKind, whose node is non-null active with node_kind CabinetNodeKind.LOCAL_BACKEND from = models.CabinetNodeKind, whose node.principal_id equals principal.principal_id, and whose node.contract_version equals the imported NODE_CONTRACT_VERSION constant. Every other result raises AuthenticationRequiredError without fallback.

## Runtime service and stream type closure

CapabilityPolicy.__init__: [DEPENDENCY_BOUNDARY] MUST initialize only the immutable closed capability catalogue and MUST NOT load credentials, persistence, dynamic operation names, read a wall clock, or retain an initialization timestamp or any other clock-derived state. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
AccessControlService.__init__: [SECURITY_BOUNDARY] MUST retain only the exact supplied per-operation CabinetUnitOfWork factory, protected credential pepper, Clock, and RuntimeSettings without opening a UoW or retaining plaintext credentials. Public facade functions project those dependencies directly into authentication_admission, capability_grants, or principal_lifecycle and never inline their mechanisms. The service MUST NOT receive a database URL, construct adapters, read environment variables, construct another clock, or probe any field/dependency reflectively with getattr, hasattr, setattr, delattr, or vars.
CardWorkspace.__init__: [DEPENDENCY_BOUNDARY] MUST retain the exact supplied per-operation CabinetUnitOfWork factory, Clock, and RuntimeSettings for durable Card revision state and wall-clock observations, and initialize no transport, credential policy, database connection, environment reader, or alternate time source. Across the module, every wall-clock timestamp that is produced comes from the retained clock, and every persisted, emitted, or compared timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, only a naive datetime is a defect, and the clock is never probed, validated, or read at construction. Every owned callable MUST call only the declared CabinetUnitOfWork and Clock operations by name; reflective attribute access over dependencies and fail-closed placeholder bodies are forbidden.
ChatGptInteractionService.__init__: [DEPENDENCY_BOUNDARY] MUST retain exactly the supplied unit_of_work_factory (opened only per proposal, confirmation or outcome read), capability_policy, card_workspace, invoice_catalogue, invoice_validation, invoice_lifecycle, project_workspace, effect_journal, source_custody, invoice_exchange, Clock, and RuntimeSettings — required for proposal, confirmation, and composite outcomes, with no generic dispatcher. Across the module, every persisted, emitted, or compared wall-clock timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, and only a naive datetime is a defect. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
EffectJournal.__init__: [DEPENDENCY_BOUNDARY] MUST retain only the exact supplied Clock; every journal operation runs inside the caller's active CabinetUnitOfWork passed as unit_of_work, so the effect record and the domain write commit as one atomic transition, and the journal MUST NOT open, commit, or roll back a transaction, receive a factory or database URL, construct adapters, read environment variables, or construct an alternate time source. Across the module, every wall-clock timestamp that is produced comes from the retained clock, and every persisted, emitted, or compared timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, only a naive datetime is a defect, and the clock is never probed, validated, or read at construction. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
InvoiceExchange.__init__: [DEPENDENCY_BOUNDARY] MUST retain the exact supplied UoW factory, retained legacy CardWorkspace, SourceCustodyService, Clock, CanonicalInvoiceSource and EffectJournal. Canonical admission and pull use only canonical_source for imported Card facts; legacy catalogue/status compatibility may retain card_workspace. No environment reads, alternate clocks, product writes, hidden adapters or local-backend network dependency are introduced.
InvoiceCatalogue.__init__: [DEPENDENCY_BOUNDARY] MUST depend only on the supplied CabinetUnitOfWorkFactory and RuntimeSettings; it MUST NOT accept a database URL, construct persistence adapters, own source bytes, call card_workspace, or perform transport work. The module-local ValidationRejectedError is constructed with the single rejecting InvoiceRejectionCode member and exposes it as its code attribute — every raiser passes the member and every reader addresses code. Across the module, every persisted, emitted, or compared wall-clock timestamp MUST be timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset; only a naive datetime is a defect. Every owned callable MUST implement its complete specified read, parsing, ordering, and return behavior; an unconditional rejection, empty result, fixed success, or other fail-closed placeholder body is forbidden. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
InvoiceCatalogue.__init__: [FIELD_ASSIGNMENT] MUST retain the exact supplied unit_of_work_factory and settings as the dependency used by every Invoice read, without opening a transaction during construction.
InvoiceValidator.__init__: [DEPENDENCY_BOUNDARY] MUST depend only on the supplied InvoiceCatalogue, used solely to call find_invoice_duplicates; it MUST NOT open a CabinetUnitOfWork, accept a database URL, construct persistence adapters, or perform any write. Across the module, every persisted, emitted, or compared wall-clock timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, and only a naive datetime is a defect. Every owned callable MUST implement its complete specified parsing, validation, ordering, and return behavior; an unconditional rejection, empty result, fixed success, or other fail-closed placeholder body is forbidden. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
InvoiceValidator.__init__: [FIELD_ASSIGNMENT] MUST retain the exact supplied catalogue as the dependency used by validate_invoice and prepare_invoice_draft.
InvoiceLifecycle.__init__: [DEPENDENCY_BOUNDARY] MUST depend only on the supplied InvoiceLifecycleUnitOfWorkFactory, canonical CardWorkspace, InvoiceCatalogue, InvoiceValidator, EffectJournal, and Clock; it MUST NOT accept a database URL, construct persistence adapters, own source bytes, perform transport work, or construct an alternate time source. Across the module, every wall-clock timestamp that is produced comes from the retained clock, and every persisted, emitted, or compared timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, only a naive datetime is a defect, and the clock is never probed, validated, or read at construction. Across the module, the imported ValidationRejectedError is constructed with the single rejecting InvoiceRejectionCode member and every reader of a caught one addresses that member only through its code attribute — no other attribute name exists on it. Every owned callable MUST implement its complete specified authorization, read, validation, mutation, and return behavior; an unconditional rejection, empty result, fixed success, or other fail-closed placeholder body is forbidden. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
InvoiceLifecycle.__init__: [FIELD_ASSIGNMENT] MUST retain the exact supplied unit_of_work_factory, card_workspace, catalogue, validator, effect_journal, and clock as the dependencies used by every Invoice mutation, without opening a transaction or reading the clock during construction.
ProjectWorkspace.__init__: [DEPENDENCY_BOUNDARY] MUST retain the per-operation CabinetUnitOfWork factory, canonical Card workspace, EffectJournal, and Clock without receiving a database URL or importing Registry master authority. Across the module, every persisted, emitted, or compared wall-clock timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, and only a naive datetime is a defect. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
RegistryReplicaService.__init__: [DEPENDENCY_BOUNDARY] MUST retain the per-operation CabinetUnitOfWork factory for received Registry replica state and never receive a database URL or mutable Registry master connection. Across the module, every persisted, emitted, or compared wall-clock timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, and only a naive datetime is a defect. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
RuntimeControl.__init__: [DEPENDENCY_BOUNDARY] MUST retain the per-operation CabinetUnitOfWork factory, SourceByteStore port, and RuntimeSettings for readiness and recovery verification without receiving or exposing database URLs or filesystem roots. Across the module, every persisted, emitted, or compared wall-clock timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, and only a naive datetime is a defect. Every owned callable MUST call the declared CabinetUnitOfWork and SourceByteStore operations directly by name — open, begin, commit, rollback and the declared methods; reflective or conditional probing of retained dependencies for attributes, methods, or context-manager support is forbidden.
SourceCustodyService.__init__: [SECURITY_BOUNDARY] MUST retain the exact supplied UoW factory, SourceByteStore, EffectJournal, Clock, RuntimeSettings and CanonicalInvoiceSource. It never constructs adapters, reads environment variables or probes the clock. Source bytes are operation-local only and pass through the canonical read boundary and retained byte-store port. All timestamps are aware UTC observations. No unconditional rejection, empty result or fixed success may replace specified custody behavior; model/dependency fields are addressed directly.
SyncGateway.__init__: [DEPENDENCY_BOUNDARY] MUST retain only the access-control service; every closed local-initiated synchronization capability enters through its own typed route, and this module never dispatches by operation name. Across the module, every persisted, emitted, or compared wall-clock timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, and only a naive datetime is a defect. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
WebGateway.__init__: [DEPENDENCY_BOUNDARY] MUST retain only the exact supplied AccessControlService, SourceCustodyService, Clock, and RuntimeSettings for the protected browser upload and download boundary and MUST NOT construct an alternate time source. Across the module, every wall-clock timestamp that is produced comes from the retained clock, and every persisted, emitted, or compared timestamp is timezone-aware: a loaded timezone-aware value with any offset is normalized to UTC through astimezone before comparison or emission and is never rejected for its offset, only a naive datetime is a defect, and the clock is never probed, validated, or read at construction. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
BinaryUpload.read_chunk: [SECURITY_BOUNDARY] MUST return at most max_bytes from the presented upload stream, return empty bytes only at exhaustion, and never expose a filesystem path.
SourceDownload.read_chunk: [SECURITY_BOUNDARY] MUST return at most max_bytes from the integrity-verified private source, return empty bytes only at exhaustion, and never expose its storage reference.
SourceDownload.describe: [RETURN_SHAPE] MUST return the exact ContentReference of the already-verified stored content without recomputing hashes, reading bytes, or exposing a storage reference.
InvoicePackageStream.read_chunk: [SECURITY_BOUNDARY] MUST return at most max_bytes from the already-issued exact package stream, return empty bytes only at exhaustion, and never reissue the package after interruption.
_source_download_response: [SECURITY_BOUNDARY] MUST return StreamingResponse over an iterator that repeatedly calls download.read_chunk(max_bytes) until empty bytes, sets the content type, attachment filename and content length from the media_type, display_filename and size_bytes of the exact ContentReference returned by download.describe(), and never exposes a storage path or reference.
_invoice_package_response: [SECURITY_BOUNDARY] MUST return StreamingResponse over an iterator that repeatedly calls package.read_chunk(max_bytes) until empty bytes and never regenerates or exposes a storage reference.
LocalFilesystemSourceByteStore.__init__: [SECURITY_BOUNDARY] MUST require one absolute non-symlink private directory, create confined staging and final subdirectories with owner-only permissions, verify they share one filesystem, and fail construction without serving traffic when confinement or atomic rename cannot be guaranteed; MUST NOT read environment variables or log the root. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
LocalFilesystemSourceByteStore.final_reference_for: [DETERMINISM_OR_ORDERING] MUST accept only one lowercase 64-hex SHA-256 value and return the unique opaque content-addressed reference beneath the final namespace; MUST NOT accept separators, relative components, or caller filenames.
LocalFilesystemSourceByteStore.stage: [SECURITY_BOUNDARY] MUST create one exclusive owner-only staging file for publication_id, write exactly content, flush file and parent directory, reopen without following symlinks, verify expected_size and SHA-256, and return only an opaque confined staging reference; mismatch removes the candidate and fails without publication.
LocalFilesystemSourceByteStore.verify: [SECURITY_BOUNDARY] MUST resolve only a canonical opaque staging or final reference beneath the configured root, reject symlinks and non-regular files, reopen and stream-hash it, and return true only for the exact expected size and SHA-256 without exposing a path.
LocalFilesystemSourceByteStore.publish: [PROVENANCE] MUST reverify the candidate, require the caller-supplied final reference to equal final_reference_for(expected_hash), atomically rename within the same filesystem without replacing different bytes, flush the final parent, and make an already-present equal verified final idempotent.
LocalFilesystemSourceByteStore.remove_staging: [FALLBACK] MUST remove only a canonical confined staging regular file, treat absence as idempotent success, reject final references, symlinks, directories, and escapes, and preserve the original application failure when used for compensation.
SourceByteStore.read_stored: [DEPENDENCY_BOUNDARY] MUST expose one verified read of previously published final bytes by opaque content-addressed reference, exact size, and exact SHA-256.
LocalFilesystemSourceByteStore.read_stored: [SECURITY_BOUNDARY] MUST resolve only a canonical opaque final reference beneath the configured root, reject symlinks and non-regular files, reopen the confined regular file, read its exact bytes, verify expected_size and the exact SHA-256 of the bytes read, and return those bytes without exposing a path; absence or any mismatch raises ValueError without returning partial data.
SourceByteStore.final_reference_for: [DEPENDENCY_BOUNDARY] MUST expose only deterministic opaque content-addressed reference derivation and no physical path.
SourceByteStore.stage: [DEPENDENCY_BOUNDARY] MUST expose one verified private staging operation for exact bytes, size, and SHA-256.
SourceByteStore.verify: [DEPENDENCY_BOUNDARY] MUST expose one confined exact byte-integrity predicate without storage details.
SourceByteStore.publish: [DEPENDENCY_BOUNDARY] MUST expose one verified same-filesystem atomic publication operation and no custody policy.
SourceByteStore.remove_staging: [DEPENDENCY_BOUNDARY] MUST expose only bounded compensation of an uncommitted staging reference.
Clock.now: [PROVENANCE] MUST return one timezone-aware UTC datetime from the clock implementation.
SystemClock.__init__: [DEPENDENCY_BOUNDARY] MUST bind no configuration, dependency, cached timestamp, or alternate time source; construction is side-effect free.
request_hash: [DETERMINISM_OR_ORDERING] MUST return the canonical digest of the whole supplied model under = rules.canonical_digest_backend.recipes.request_hash; equal models always digest equally.
catalogue_content_digest: [DETERMINISM_OR_ORDERING] MUST return the canonical digest of the supplied delivery under = rules.canonical_digest_backend.recipes.catalogue_content_digest; the delivery's own content_hash and idempotency_key never enter the digest.
manifest_hash: [DETERMINISM_OR_ORDERING] MUST return the canonical digest of the supplied manifest under = rules.canonical_digest_backend.recipes.manifest_hash; the manifest's own manifest_id and manifest_hash never enter the digest.
canonical_json_text_digest: [DETERMINISM_OR_ORDERING] MUST return the canonical digest of the parsed JSON document under = rules.canonical_digest_backend.recipes.canonical_json_text_digest; two spellings of one document digest equally.
string_tuple_digest: [DETERMINISM_OR_ORDERING] MUST return the canonical digest of the ordered strings under = rules.canonical_digest_backend.recipes.string_tuple_digest; order is significant.
SystemClock.now: [PROVENANCE] MUST return the current system wall-clock time using datetime.now(timezone.utc).
CabinetUnitOfWork.begin: [DEPENDENCY_BOUNDARY] MUST start one explicit operation-scoped metadata transaction and reject a nested active transaction.
CabinetUnitOfWork.commit: [DEPENDENCY_BOUNDARY] MUST commit exactly the active metadata transaction once and release its connection.
CabinetUnitOfWork.rollback: [FALLBACK] MUST roll back the active metadata transaction idempotently, release its connection, and preserve the original application failure.
PostgresCabinetUnitOfWork.__init__: [SECURITY_BOUNDARY] MUST bind only the supplied PostgreSQL URL, validate connectivity and schema compatibility, treat it as secret, and never read environment variables or log credentials. Reflective attribute access over models, dependencies, or self (getattr, hasattr, setattr, delattr, vars) is forbidden across the module; every declared field and operation is addressed directly by name.
PostgresCabinetUnitOfWork.begin: [DEPENDENCY_BOUNDARY] MUST open one PostgreSQL transaction for the exact application operation and reject nested begin.
PostgresCabinetUnitOfWork.commit: [BEHAVIOR] MUST commit the active PostgreSQL transaction exactly once and release its connection without exposing partial state.
PostgresCabinetUnitOfWork.rollback: [FALLBACK] MUST roll back idempotently, release the connection, and preserve the original application failure.
CabinetUnitOfWork.lock_effect: [DEPENDENCY_BOUNDARY] MUST serialize the exact effect identity inside the active transaction before mutable state is read.
CabinetUnitOfWork.load_effect: [DEPENDENCY_BOUNDARY] MUST return only the exact persisted effect or None.
CabinetUnitOfWork.load_effect_by_idempotency: [DEPENDENCY_BOUNDARY] MUST return only the effect bound to the exact principal and idempotency key or None and reject duplicate persisted matches.
CabinetUnitOfWork.insert_effect: [DEPENDENCY_BOUNDARY] MUST append one effect reservation without replacement inside the active transaction.
CabinetUnitOfWork.update_effect_result: [DEPENDENCY_BOUNDARY] MUST update only completion state and result fields of the exact existing effect.
create_cabinet_schema: [SECURITY_BOUNDARY] MUST apply only the closed monotonic PostgreSQL schema before traffic, fail on incompatible schema or connection, treat database_url as secret, and never drop accepted evidence.
PostgresCabinetUnitOfWork.lock_effect: [BEHAVIOR] MUST acquire a transaction-scoped PostgreSQL advisory lock for the exact effect_id before effect state is read or changed.
PostgresCabinetUnitOfWork.load_effect: [BEHAVIOR] MUST return the exact CabinetEffect row or None without synthesizing defaults.
PostgresCabinetUnitOfWork.load_effect_by_idempotency: [BEHAVIOR] MUST return the unique row for principal_id and idempotency_key or None and treat multiple rows as storage failure.
PostgresCabinetUnitOfWork.insert_effect: [PROVENANCE] MUST append one complete CabinetEffect reservation; duplicate effect identity or principal-scoped idempotency identity fails without replacement.
PostgresCabinetUnitOfWork.update_effect_result: [PROVENANCE] MUST update status, result_revision, safe_error_code, and completed_at for the exact existing effect, require an existing row, and never change request identity or creation evidence.
CabinetUnitOfWorkFactory.open: [DEPENDENCY_BOUNDARY] MUST return one fresh inactive CabinetUnitOfWork for exactly one application operation; callers begin, commit or roll it back and never share it with another request.

## Deterministic PostgreSQL UoW and composition closure

CabinetUnitOfWork.lock_card: [DEPENDENCY_BOUNDARY] MUST acquire only the transaction-scoped cabinet.card lock for typed key(s) card_id inside the already active operation UoW before dependent reads or writes; MUST NOT open, commit, retry, or apply domain policy on its own.
CabinetUnitOfWork.load_current_card_reference: [PROVENANCE] MUST read only the exact persisted CardRevisionReference row selected by the declared typed filter from card_current_revisions inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.list_current_card_references_by_type: [DETERMINISM_OR_ORDERING] MUST return only persisted CardRevisionReference rows selected from card_current_revisions by the declared bounded filter and IR ordering inside the active operation UoW; MUST NOT infer additional membership or mutate storage.
CabinetUnitOfWork.load_card_revision: [PROVENANCE] MUST read only the exact persisted CanonicalCardRevision row selected by the declared typed filter from card_revisions inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_card_revision: [PROVENANCE] MUST insert the exact supplied CanonicalCardRevision projection into card_revisions inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.insert_current_card_reference: [PROVENANCE] MUST insert the exact supplied CardRevisionReference projection into card_current_revisions inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_current_card_reference: [PROVENANCE] MUST update only the declared mutable field(s) card_type, content_hash, card_format_version, observed_status, observed_at on the existing CardRevisionReference row selected by the IR filter in card_current_revisions inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.lock_principal: [DEPENDENCY_BOUNDARY] MUST acquire only the transaction-scoped cabinet.principal lock for typed key(s) principal_id inside the already active operation UoW before dependent reads or writes; MUST NOT open, commit, retry, or apply domain policy on its own.
CabinetUnitOfWork.load_principal: [PROVENANCE] MUST read only the exact persisted CabinetPrincipal row selected by the declared typed filter from cabinet_principals inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.list_principals_by_kind: [DETERMINISM_OR_ORDERING] MUST return only persisted CabinetPrincipal rows matching the exact supplied principal_kind in ascending principal_id order inside the active operation UoW; MUST NOT infer lifecycle policy, omit matching rows, mutate storage, or commit independently.
CabinetUnitOfWork.insert_principal: [PROVENANCE] MUST insert the exact supplied CabinetPrincipal projection into cabinet_principals inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_principal_status: [PROVENANCE] MUST update only the declared mutable field(s) status, revoked_at on the existing CabinetPrincipal row selected by the IR filter in cabinet_principals inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.lock_node: [DEPENDENCY_BOUNDARY] MUST acquire only the transaction-scoped cabinet.node lock for typed key(s) node_id inside the already active operation UoW before dependent reads or writes; MUST NOT open, commit, retry, or apply domain policy on its own.
CabinetUnitOfWork.load_node: [PROVENANCE] MUST read only the exact persisted CabinetNodeIdentity row selected by the declared typed filter from cabinet_nodes inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_node: [PROVENANCE] MUST insert the exact supplied CabinetNodeIdentity projection into cabinet_nodes inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_node_status: [PROVENANCE] MUST update only the declared mutable field(s) status, contract_version, revoked_at on the existing CabinetNodeIdentity row selected by the IR filter in cabinet_nodes inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.lock_credential: [DEPENDENCY_BOUNDARY] MUST acquire only the transaction-scoped cabinet.credential lock for typed key(s) credential_id inside the already active operation UoW before dependent reads or writes; MUST NOT open, commit, retry, or apply domain policy on its own.
CabinetUnitOfWork.load_credential: [PROVENANCE] MUST read only the exact persisted AccessCredentialRecord row selected by the declared typed filter from access_credentials inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.list_credentials_for_subject: [DETERMINISM_OR_ORDERING] MUST return only persisted AccessCredentialRecord rows selected from access_credentials by the declared bounded filter and IR ordering inside the active operation UoW; MUST NOT infer additional membership or mutate storage.
CabinetUnitOfWork.insert_credential: [PROVENANCE] MUST insert the exact supplied AccessCredentialRecord projection into access_credentials inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_credential: [PROVENANCE] MUST update only the declared mutable field(s) status, revoked_at, last_authenticated_at on the existing AccessCredentialRecord row selected by the IR filter in access_credentials inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.load_capability_grant: [PROVENANCE] MUST read only the exact persisted CapabilityGrantRecord row selected by the declared typed filter from capability_grants inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.list_capability_grants_for_principal: [DETERMINISM_OR_ORDERING] MUST return only persisted CapabilityGrantRecord rows selected from capability_grants by the declared bounded filter and IR ordering inside the active operation UoW; MUST NOT infer additional membership or mutate storage.
CabinetUnitOfWork.insert_capability_grant: [PROVENANCE] MUST insert the exact supplied CapabilityGrantRecord projection into capability_grants inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.lock_abuse_context: [DEPENDENCY_BOUNDARY] MUST acquire only the transaction-scoped cabinet.auth_abuse lock for typed key(s) abuse_context_hash inside the already active operation UoW before dependent reads or writes; MUST NOT open, commit, retry, or apply domain policy on its own.
CabinetUnitOfWork.load_throttle_state: [PROVENANCE] MUST read only the exact persisted AuthenticationThrottleState row selected by the declared typed filter from authentication_throttle_states inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.upsert_throttle_state: [PROVENANCE] MUST perform only the IR-declared upsert for AuthenticationThrottleState in authentication_throttle_states, using conflict key(s) abuse_context_hash and changing only credential_id, consecutive_failures, delay_until, blocked_until, updated_at on conflict inside the active operation UoW; MUST NOT broaden the conflict identity or apply business policy.
CabinetUnitOfWork.insert_security_audit: [PROVENANCE] MUST insert the exact supplied SecurityAuditRecord projection into security_audit_records inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.lock_handoff: [DEPENDENCY_BOUNDARY] MUST acquire only the transaction-scoped cabinet.source_handoff lock for typed key(s) handoff_id inside the already active operation UoW before dependent reads or writes; MUST NOT open, commit, retry, or apply domain policy on its own.
CabinetUnitOfWork.load_handoff: [PROVENANCE] MUST read only the exact persisted SourceUploadHandoffRecord row selected by the declared typed filter from source_upload_handoffs inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_handoff: [PROVENANCE] MUST insert the exact supplied SourceUploadHandoffRecord projection into source_upload_handoffs inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_handoff: [PROVENANCE] MUST update only the declared mutable field(s) status, consumed_at, revoked_at on the existing SourceUploadHandoffRecord row selected by the IR filter in source_upload_handoffs inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.lock_source_custody: [DEPENDENCY_BOUNDARY] MUST acquire only the transaction-scoped cabinet.source_custody lock for typed key(s) card_id, source_id inside the already active operation UoW before dependent reads or writes; MUST NOT open, commit, retry, or apply domain policy on its own.
CabinetUnitOfWork.load_source_custody: [PROVENANCE] MUST read only the exact persisted SourceCustodyRecord row selected by the declared typed filter from source_custody_records inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_source_custody: [PROVENANCE] MUST insert the exact supplied SourceCustodyRecord projection into source_custody_records inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_source_custody: [PROVENANCE] MUST update only the declared mutable field(s) content, custody_status, accepted_at, rejected_at, released_at, rejection_reason, actor on the existing SourceCustodyRecord row selected by the IR filter in source_custody_records inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.load_source_publication: [PROVENANCE] MUST read only the exact persisted SourceBytePublication row selected by the declared typed filter from source_byte_publications inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.list_source_publications_in_states: [DETERMINISM_OR_ORDERING] MUST return only persisted SourceBytePublication rows selected from source_byte_publications by the declared bounded filter and IR ordering inside the active operation UoW; MUST NOT infer additional membership or mutate storage.
CabinetUnitOfWork.insert_source_publication: [PROVENANCE] MUST insert the exact supplied SourceBytePublication projection into source_byte_publications inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_source_publication: [PROVENANCE] MUST update only the declared mutable field(s) state, updated_at, failure_code on the existing SourceBytePublication row selected by the IR filter in source_byte_publications inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.load_transfer_manifest: [PROVENANCE] MUST read only the exact persisted InvoiceTransferManifest row selected by the declared typed filter from invoice_transfer_manifests inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_transfer_manifest: [PROVENANCE] MUST insert the exact supplied InvoiceTransferManifest projection into invoice_transfer_manifests inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.load_working_set_record: [PROVENANCE] MUST read only the exact persisted InvoiceWorkingSetRecord row selected by the declared typed filter from invoice_working_sets inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.list_available_working_set_records: [DETERMINISM_OR_ORDERING] MUST return only persisted InvoiceWorkingSetRecord rows selected from invoice_working_sets by the declared bounded filter and IR ordering inside the active operation UoW; MUST NOT infer additional membership or mutate storage.
CabinetUnitOfWork.insert_working_set_record: [PROVENANCE] MUST insert the exact supplied InvoiceWorkingSetRecord projection into invoice_working_sets inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.insert_line_capture_evidence: [PROVENANCE] MUST insert the exact supplied InvoiceLineCaptureEvidence projection into invoice_line_capture_evidence inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.load_line_capture_evidence: [PROVENANCE] MUST read only the exact persisted InvoiceLineCaptureEvidence row selected by the declared typed filter (invoice_id, card_content_hash) from invoice_line_capture_evidence inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.update_working_set_status: [PROVENANCE] MUST update only the declared mutable field(s) status, released_at on the existing InvoiceWorkingSetRecord row selected by the IR filter in invoice_working_sets inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.lock_transfer_issuance: [DEPENDENCY_BOUNDARY] MUST acquire only the transaction-scoped cabinet.transfer_issuance lock for typed key(s) issuance_id inside the already active operation UoW before dependent reads or writes; MUST NOT open, commit, retry, or apply domain policy on its own.
CabinetUnitOfWork.load_transfer_issuance: [PROVENANCE] MUST read only the exact persisted InvoiceTransferIssuance row selected by the declared typed filter from invoice_transfer_issuances inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.load_transfer_issuance_by_idempotency: [PROVENANCE] MUST read only the exact persisted InvoiceTransferIssuance row selected by the declared typed filter from invoice_transfer_issuances inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_transfer_issuance: [PROVENANCE] MUST insert the exact supplied InvoiceTransferIssuance projection into invoice_transfer_issuances inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_transfer_issuance: [PROVENANCE] MUST update only the declared mutable field(s) status, issued_at, acknowledged_at, receipt_result, safe_error_code on the existing InvoiceTransferIssuance row selected by the IR filter in invoice_transfer_issuances inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.load_transfer_receipt: [PROVENANCE] MUST read only the exact persisted InvoiceTransferReceiptRecord row selected by the declared typed filter from invoice_transfer_receipts inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_transfer_receipt: [PROVENANCE] MUST insert the exact supplied InvoiceTransferReceiptRecord projection into invoice_transfer_receipts inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.load_synchronization_conflict: [PROVENANCE] MUST read only the exact persisted SynchronizationConflict row selected by the declared typed filter from synchronization_conflicts inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_synchronization_conflict: [PROVENANCE] MUST insert the exact supplied SynchronizationConflict projection into synchronization_conflicts inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_synchronization_conflict: [PROVENANCE] MUST update only the declared mutable field(s) status, resolution_evidence, resolved_at on the existing SynchronizationConflict row selected by the IR filter in synchronization_conflicts inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.load_assignment_observation: [PROVENANCE] MUST read only the exact persisted CardObjectAssignmentObservation row selected by the declared typed filter from card_assignment_observations inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_assignment_observation: [PROVENANCE] MUST insert the exact supplied CardObjectAssignmentObservation projection into card_assignment_observations inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.insert_connection_observation: [PROVENANCE] MUST insert the exact supplied LocalBackendConnectionObservationRecord projection into local_backend_connection_observations inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.lock_registry_current: [DEPENDENCY_BOUNDARY] MUST acquire only the transaction-scoped cabinet.registry_current lock for typed key(s) node_id inside the already active operation UoW before dependent reads or writes; MUST NOT open, commit, retry, or apply domain policy on its own.
CabinetUnitOfWork.load_registry_snapshot: [PROVENANCE] MUST read only the exact persisted RegistryCatalogueSnapshot row selected by the declared typed filter from registry_catalogue_snapshots inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_registry_snapshot: [PROVENANCE] MUST insert the exact supplied RegistryCatalogueSnapshot projection into registry_catalogue_snapshots inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.load_registry_replica: [PROVENANCE] MUST read only the exact persisted RegistryCatalogueReplica row selected by the declared typed filter from registry_catalogue_replicas inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_registry_replica: [PROVENANCE] MUST insert the exact supplied RegistryCatalogueReplica projection into registry_catalogue_replicas inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_registry_replica: [PROVENANCE] MUST update only the declared mutable field(s) status, verified_at, current_at, superseded_at, rejected_at, rejection_reason on the existing RegistryCatalogueReplica row selected by the IR filter in registry_catalogue_replicas inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.load_registry_publication_by_idempotency: [PROVENANCE] MUST read only the exact persisted RegistryCataloguePublicationRecord row selected by the declared typed filter from registry_catalogue_publications inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_registry_publication: [PROVENANCE] MUST insert the exact supplied RegistryCataloguePublicationRecord projection into registry_catalogue_publications inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_registry_publication: [PROVENANCE] MUST update only the declared mutable field(s) status, acknowledged_at, safe_error_code on the existing RegistryCataloguePublicationRecord row selected by the IR filter in registry_catalogue_publications inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.load_registry_current_selector: [PROVENANCE] MUST read only the exact persisted RegistryCatalogueCurrentSelector row selected by the declared typed filter from registry_catalogue_current_selectors inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_registry_current_selector: [PROVENANCE] MUST insert the exact supplied RegistryCatalogueCurrentSelector projection into registry_catalogue_current_selectors inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_registry_current_selector: [PROVENANCE] MUST update only the declared mutable field(s) catalogue_id, content_hash, selected_at on the existing RegistryCatalogueCurrentSelector row selected by the IR filter in registry_catalogue_current_selectors inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.load_backup_restore_drill: [PROVENANCE] MUST read only the exact persisted BackupRestoreDrillRecord row selected by the declared typed filter from backup_restore_drills inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_backup_restore_drill: [PROVENANCE] MUST insert the exact supplied BackupRestoreDrillRecord projection into backup_restore_drills inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CabinetUnitOfWork.update_backup_restore_drill: [PROVENANCE] MUST update only the declared mutable field(s) status, verified_card_revisions, verified_source_hashes, completed_at, safe_error_code on the existing BackupRestoreDrillRecord row selected by the IR filter in backup_restore_drills inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
CabinetUnitOfWork.load_release_evidence: [PROVENANCE] MUST read only the exact persisted VpsReleaseEvidence row selected by the declared typed filter from vps_release_evidence inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CabinetUnitOfWork.insert_release_evidence: [PROVENANCE] MUST insert the exact supplied VpsReleaseEvidence projection into vps_release_evidence inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
InvoiceLifecycleUnitOfWork.begin: [DEPENDENCY_BOUNDARY] MUST start one explicit operation-scoped metadata transaction and reject a nested active transaction.
InvoiceLifecycleUnitOfWork.commit: [DEPENDENCY_BOUNDARY] MUST commit exactly the active metadata transaction once and release its connection.
InvoiceLifecycleUnitOfWork.rollback: [FALLBACK] MUST roll back the active metadata transaction idempotently, release its connection, and preserve the original application failure.
InvoiceLifecycleUnitOfWork.lock_card: [DEPENDENCY_BOUNDARY] MUST acquire only the transaction-scoped cabinet.card lock for typed key(s) card_id inside the already active operation UoW before dependent reads or writes; MUST NOT open, commit, retry, or apply domain policy on its own.
InvoiceLifecycleUnitOfWork.load_current_card_reference: [PROVENANCE] MUST read only the exact persisted CardRevisionReference row selected by the declared typed filter from card_current_revisions inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
InvoiceLifecycleUnitOfWork.load_source_custody: [PROVENANCE] MUST read only the exact persisted SourceCustodyRecord row selected by the declared typed filter from source_custody_records inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
InvoiceLifecycleUnitOfWork.load_transfer_manifest: [PROVENANCE] MUST read only the exact persisted InvoiceTransferManifest row selected by the declared typed filter from invoice_transfer_manifests inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
InvoiceLifecycleUnitOfWork.load_working_set_record: [PROVENANCE] MUST read only the exact persisted InvoiceWorkingSetRecord row selected by the declared typed filter from invoice_working_sets inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
InvoiceLifecycleUnitOfWork.insert_transfer_manifest: [PROVENANCE] MUST insert the exact supplied InvoiceTransferManifest projection into invoice_transfer_manifests inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
InvoiceLifecycleUnitOfWork.insert_working_set_record: [PROVENANCE] MUST insert the exact supplied InvoiceWorkingSetRecord projection into invoice_working_sets inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
InvoiceLifecycleUnitOfWork.insert_line_capture_evidence: [PROVENANCE] MUST insert the exact supplied InvoiceLineCaptureEvidence projection into invoice_line_capture_evidence inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
InvoiceLifecycleUnitOfWork.load_line_capture_evidence: [PROVENANCE] MUST read only the exact persisted InvoiceLineCaptureEvidence row selected by the declared typed filter (invoice_id, card_content_hash) from invoice_line_capture_evidence inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
InvoiceLifecycleUnitOfWork.lock_effect: [DEPENDENCY_BOUNDARY] MUST serialize the exact effect identity inside the active transaction before mutable state is read.
InvoiceLifecycleUnitOfWork.load_effect: [DEPENDENCY_BOUNDARY] MUST return only the exact persisted effect or None.
InvoiceLifecycleUnitOfWork.load_effect_by_idempotency: [DEPENDENCY_BOUNDARY] MUST return only the effect bound to the exact principal and idempotency key or None and reject duplicate persisted matches.
InvoiceLifecycleUnitOfWork.insert_effect: [DEPENDENCY_BOUNDARY] MUST append one effect reservation without replacement inside the active transaction.
InvoiceLifecycleUnitOfWork.update_effect_result: [DEPENDENCY_BOUNDARY] MUST update only completion state and result fields of the exact existing effect.
InvoiceLifecycleUnitOfWork.load_card_revision: [PROVENANCE] MUST read only the exact persisted CanonicalCardRevision row selected by the declared typed filter from card_revisions inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
InvoiceLifecycleUnitOfWork.insert_card_revision: [PROVENANCE] MUST insert the exact supplied CanonicalCardRevision projection into card_revisions inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
InvoiceLifecycleUnitOfWork.insert_current_card_reference: [PROVENANCE] MUST insert the exact supplied CardRevisionReference projection into card_current_revisions inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
InvoiceLifecycleUnitOfWork.update_current_card_reference: [PROVENANCE] MUST update only the declared mutable field(s) card_type, content_hash, card_format_version, observed_status, observed_at on the existing CardRevisionReference row selected by the IR filter in card_current_revisions inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
InvoiceLifecycleUnitOfWork.list_available_working_set_records: [DETERMINISM_OR_ORDERING] MUST return only persisted InvoiceWorkingSetRecord rows selected from invoice_working_sets by the declared bounded filter and IR ordering inside the active operation UoW; MUST NOT infer additional membership or mutate storage.
EffectJournalUnitOfWork.lock_effect: [DEPENDENCY_BOUNDARY] MUST serialize the exact effect identity inside the active transaction before mutable state is read.
EffectJournalUnitOfWork.load_effect: [DEPENDENCY_BOUNDARY] MUST return only the exact persisted effect or None.
EffectJournalUnitOfWork.load_effect_by_idempotency: [DEPENDENCY_BOUNDARY] MUST return only the effect bound to the exact principal and idempotency key or None and reject duplicate persisted matches.
EffectJournalUnitOfWork.insert_effect: [DEPENDENCY_BOUNDARY] MUST append one effect reservation without replacement inside the active transaction.
EffectJournalUnitOfWork.update_effect_result: [DEPENDENCY_BOUNDARY] MUST update only completion state and result fields of the exact existing effect.
CardRevisionCommitUnitOfWork.lock_card: [DEPENDENCY_BOUNDARY] MUST acquire only the transaction-scoped cabinet.card lock for typed key(s) card_id inside the already active operation UoW before dependent reads or writes; MUST NOT open, commit, retry, or apply domain policy on its own.
CardRevisionCommitUnitOfWork.load_current_card_reference: [PROVENANCE] MUST read only the exact persisted CardRevisionReference row selected by the declared typed filter from card_current_revisions inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
CardRevisionCommitUnitOfWork.insert_card_revision: [PROVENANCE] MUST insert the exact supplied CanonicalCardRevision projection into card_revisions inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CardRevisionCommitUnitOfWork.insert_current_card_reference: [PROVENANCE] MUST insert the exact supplied CardRevisionReference projection into card_current_revisions inside the active operation UoW and let uniqueness/constraint conflicts remain visible to the caller; MUST NOT upsert, replace prior evidence, or commit independently.
CardRevisionCommitUnitOfWork.update_current_card_reference: [PROVENANCE] MUST update only the declared mutable field(s) card_type, content_hash, card_format_version, observed_status, observed_at on the existing CardRevisionReference row selected by the IR filter in card_current_revisions inside the active operation UoW; MUST NOT insert a missing row, change immutable binding fields, or commit independently.
InvoiceRevisionReadUnitOfWork.load_current_card_reference: [PROVENANCE] MUST read only the exact persisted CardRevisionReference row selected by the declared typed filter from card_current_revisions inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
InvoiceRevisionReadUnitOfWork.load_card_revision: [PROVENANCE] MUST read only the exact persisted CanonicalCardRevision row selected by the declared typed filter from card_revisions inside the active operation UoW; MUST return stored facts without substituting another identity/version or applying lifecycle policy.
InvoiceRevisionReadUnitOfWork.list_available_working_set_records: [DETERMINISM_OR_ORDERING] MUST return only persisted InvoiceWorkingSetRecord rows selected from invoice_working_sets by the declared bounded filter and IR ordering inside the active operation UoW; MUST NOT infer additional membership or mutate storage.
InvoiceLifecycleUnitOfWorkFactory.open: [DEPENDENCY_BOUNDARY] MUST return one fresh inactive InvoiceLifecycleUnitOfWork for exactly one application operation; callers begin, commit or roll it back and never share it with another request.
PostgresCabinetUnitOfWorkFactory.__init__: [DEPENDENCY_BOUNDARY] MUST bind only the typed database_url supplied by the composition root from settings.database_url; MUST NOT open a transaction, read domain state, or provide a fallback database.
PostgresCabinetUnitOfWorkFactory.open: [DEPENDENCY_BOUNDARY] MUST construct and return one fresh PostgresCabinetUnitOfWork for one application operation so singleton services never share an active transaction; connection/UoW construction MUST remain outside domain services.
create_cabinet_web_app: [ORCHESTRATION] MUST be the sole Cabinet Web composition root: call `capability:runtime_settings.load_runtime_settings` exactly once before any other construction, retain that one immutable settings snapshot, pass settings.database_url to the PostgreSQL factory, run PostgreSQL migration/connectivity checks before traffic, construct the PostgreSQL UoW factory, the protected filesystem byte store from settings.source_store_root_path, and exactly one SystemClock, pass settings.credential_pepper only to AccessControlService, then perform source-publication recovery before traffic: open one CabinetUnitOfWork through the factory's open, call begin, load every SourceBytePublication still in a staging, pending or recovering state through list_source_publications_in_states, mark each failed with failure_code startup_recovery through update_source_publication, commit that unit of work exactly once and roll it back on every exception, supply that same Clock instance to CardWorkspace, AccessControlService, InvoiceLifecycle, InvoiceExchange, EffectJournal, SourceCustodyService, and WebGateway, inject the same settings snapshot into every declared settings consumer and wire every other accepted service/gateway dependency, then delegate the completed graph to create_app; MUST fail closed instead of inventing adapters, defaults, alternate clocks, or local-backend reachability. Every timestamp written during that recovery (the updated_at of a re-marked publication) is one value of that single SystemClock now() taken once before the loop; datetime.now() or any other time source is never called in this module.
create_cabinet_web_app: [DEPENDENCY_BOUNDARY] MUST import Clock, RuntimeSettings, and CabinetUnitOfWork interfaces only from models, load_runtime_settings only from runtime_settings, PostgresCabinetUnitOfWork and create_cabinet_schema only from cabinet_persistence, LocalFilesystemSourceByteStore only from source_byte_store, every service and gateway class only from the module that owns it, and create_app only from api; SystemClock is the composition-root implementation of Clock. It MUST NOT import from any other internal path, invent a persistence or adapter module, construct a unit of work outside PostgresCabinetUnitOfWorkFactory, or construct service-local clocks.
create_invoice_draft: [PROVENANCE] MUST, after require_mutation_authorization, open one InvoiceLifecycleUnitOfWork through unit_of_work_factory.open and begin it, and inside that single transaction first call begin_effect(self.effect_journal, unit_of_work, BeginEffectRequest(idempotency_key=command.effect_id, operation_kind=authorization.capability, principal_id=authorization.principal_id, actor=command.actor, target_identity=command.draft.invoice.id, expected_revision=None, request_hash=the imported request_hash(command) — the imported REQUEST_HASH_ALGORITHM constant names that procedure in stored records and is never passed to hashlib)). If reservation.replayed is True and reservation.effect.status == EffectStatus.COMMITTED from = models.EffectStatus it MUST roll back and return InvoiceMutationResult(effect_id=command.effect_id, revision=reservation.effect.result_revision, status=reservation.effect.result_revision.observed_status, replayed=True, issues=(), safe_error_code=None) without any mutation. Every Card read and the Card commit use that same unit_of_work; after the Card commit it MUST call commit_effect(self.effect_journal, unit_of_work, command.effect_id, EffectResult(effect_id=command.effect_id, status= EffectStatus.COMMITTED from = models.EffectStatus, result_revision=commit_result.revision, safe_error_code=None, completed_at=operation_utc_time)) and then commit the unit_of_work exactly once; every exception rolls the unit_of_work back before propagating.
update_invoice_draft: [PROVENANCE] MUST, after require_mutation_authorization, open one InvoiceLifecycleUnitOfWork through unit_of_work_factory.open and begin it, and inside that single transaction first call begin_effect(self.effect_journal, unit_of_work, BeginEffectRequest(idempotency_key=command.effect_id, operation_kind=authorization.capability, principal_id=authorization.principal_id, actor=command.actor, target_identity=command.invoice_id, expected_revision=command.expected_revision, request_hash=the imported request_hash(command) — the imported REQUEST_HASH_ALGORITHM constant names that procedure in stored records and is never passed to hashlib)). If reservation.replayed is True and reservation.effect.status == EffectStatus.COMMITTED from = models.EffectStatus it MUST roll back and return InvoiceMutationResult(effect_id=command.effect_id, revision=reservation.effect.result_revision, status=reservation.effect.result_revision.observed_status, replayed=True, issues=(), safe_error_code=None) without any mutation. Every Card read and the Card commit use that same unit_of_work; after the Card commit it MUST call commit_effect(self.effect_journal, unit_of_work, command.effect_id, EffectResult(effect_id=command.effect_id, status= EffectStatus.COMMITTED from = models.EffectStatus, result_revision=commit_result.revision, safe_error_code=None, completed_at=operation_utc_time)) and then commit the unit_of_work exactly once; every exception rolls the unit_of_work back before propagating.
confirm_invoice: [PROVENANCE] MUST, inside the single transaction it opens and immediately after begin, first call begin_effect(self.effect_journal, unit_of_work, BeginEffectRequest(idempotency_key=command.effect_id, operation_kind=authorization.capability, principal_id=authorization.principal_id, actor=command.actor, target_identity=command.invoice_id, expected_revision=command.expected_revision, request_hash=the imported request_hash(command) — the imported REQUEST_HASH_ALGORITHM constant names that procedure in stored records and is never passed to hashlib)). If reservation.replayed is True and reservation.effect.status == EffectStatus.COMMITTED from = models.EffectStatus it MUST roll back and return InvoiceMutationResult(effect_id=command.effect_id, revision=reservation.effect.result_revision, status=reservation.effect.result_revision.observed_status, replayed=True, issues=(), safe_error_code=None) without any mutation. If reservation.replayed is False, or reservation.effect.status != EffectStatus.PREPARED from = models.EffectStatus, or reservation.effect.operation_kind != authorization.capability, it MUST roll back and raise ValidationRejectedError with InvoiceRejectionCode.CONFIRMATION from = models.InvoiceRejectionCode: a consequential effect proceeds only against the reservation the plugin's explicit confirmation left in the journal. Every Card read and the Card commit use that same unit_of_work; after the Card commit it MUST call commit_effect(self.effect_journal, unit_of_work, command.effect_id, EffectResult(effect_id=command.effect_id, status= EffectStatus.COMMITTED from = models.EffectStatus, result_revision=commit_result.revision, safe_error_code=None, completed_at=operation_utc_time)) and then commit the unit_of_work exactly once; every exception rolls the unit_of_work back before propagating.
record_invoice_payment: [PROVENANCE] MUST, after require_mutation_authorization, open one InvoiceLifecycleUnitOfWork through unit_of_work_factory.open and begin it, and inside that single transaction first call begin_effect(self.effect_journal, unit_of_work, BeginEffectRequest(idempotency_key=command.effect_id, operation_kind=authorization.capability, principal_id=authorization.principal_id, actor=command.actor, target_identity=command.invoice_id, expected_revision=command.expected_revision, request_hash=the imported request_hash(command) — the imported REQUEST_HASH_ALGORITHM constant names that procedure in stored records and is never passed to hashlib)). If reservation.replayed is True and reservation.effect.status == EffectStatus.COMMITTED from = models.EffectStatus it MUST roll back and return InvoiceMutationResult(effect_id=command.effect_id, revision=reservation.effect.result_revision, status=reservation.effect.result_revision.observed_status, replayed=True, issues=(), safe_error_code=None) without any mutation. If reservation.replayed is False, or reservation.effect.status != EffectStatus.PREPARED from = models.EffectStatus, or reservation.effect.operation_kind != authorization.capability, it MUST roll back and raise ValidationRejectedError with InvoiceRejectionCode.CONFIRMATION from = models.InvoiceRejectionCode: a consequential effect proceeds only against the reservation the plugin's explicit confirmation left in the journal. Every Card read and the Card commit use that same unit_of_work; after the Card commit it MUST call commit_effect(self.effect_journal, unit_of_work, command.effect_id, EffectResult(effect_id=command.effect_id, status= EffectStatus.COMMITTED from = models.EffectStatus, result_revision=commit_result.revision, safe_error_code=None, completed_at=operation_utc_time)) and then commit the unit_of_work exactly once; every exception rolls the unit_of_work back before propagating.
attach_invoice_source_metadata: [PROVENANCE] MUST, after require_mutation_authorization, open one InvoiceLifecycleUnitOfWork through unit_of_work_factory.open and begin it, and inside that single transaction first call begin_effect(self.effect_journal, unit_of_work, BeginEffectRequest(idempotency_key=command.effect_id, operation_kind=authorization.capability, principal_id=authorization.principal_id, actor=command.actor, target_identity=command.invoice_id, expected_revision=command.expected_revision, request_hash=the imported request_hash(command) — the imported REQUEST_HASH_ALGORITHM constant names that procedure in stored records and is never passed to hashlib)). If reservation.replayed is True and reservation.effect.status == EffectStatus.COMMITTED from = models.EffectStatus it MUST roll back and return InvoiceMutationResult(effect_id=command.effect_id, revision=reservation.effect.result_revision, status=reservation.effect.result_revision.observed_status, replayed=True, issues=(), safe_error_code=None) without any mutation. Every Card read and the Card commit use that same unit_of_work; after the Card commit it MUST call commit_effect(self.effect_journal, unit_of_work, command.effect_id, EffectResult(effect_id=command.effect_id, status= EffectStatus.COMMITTED from = models.EffectStatus, result_revision=commit_result.revision, safe_error_code=None, completed_at=operation_utc_time)) and then commit the unit_of_work exactly once; every exception rolls the unit_of_work back before propagating.
archive_invoice: [PROVENANCE] MUST, after require_mutation_authorization, open one InvoiceLifecycleUnitOfWork through unit_of_work_factory.open and begin it, and inside that single transaction first call begin_effect(self.effect_journal, unit_of_work, BeginEffectRequest(idempotency_key=command.effect_id, operation_kind=authorization.capability, principal_id=authorization.principal_id, actor=command.actor, target_identity=command.invoice_id, expected_revision=command.expected_revision, request_hash=the imported request_hash(command) — the imported REQUEST_HASH_ALGORITHM constant names that procedure in stored records and is never passed to hashlib)). If reservation.replayed is True and reservation.effect.status == EffectStatus.COMMITTED from = models.EffectStatus it MUST roll back and return InvoiceMutationResult(effect_id=command.effect_id, revision=reservation.effect.result_revision, status=reservation.effect.result_revision.observed_status, replayed=True, issues=(), safe_error_code=None) without any mutation. If reservation.replayed is False, or reservation.effect.status != EffectStatus.PREPARED from = models.EffectStatus, or reservation.effect.operation_kind != authorization.capability, it MUST roll back and raise ValidationRejectedError with InvoiceRejectionCode.CONFIRMATION from = models.InvoiceRejectionCode: a consequential effect proceeds only against the reservation the plugin's explicit confirmation left in the journal. Every Card read and the Card commit use that same unit_of_work; after the Card commit it MUST call commit_effect(self.effect_journal, unit_of_work, command.effect_id, EffectResult(effect_id=command.effect_id, status= EffectStatus.COMMITTED from = models.EffectStatus, result_revision=commit_result.revision, safe_error_code=None, completed_at=operation_utc_time)) and then commit the unit_of_work exactly once; every exception rolls the unit_of_work back before propagating.
attach_project_estimate: [PROVENANCE] MUST, after requiring authorization.allowed, authorization.capability present in the imported CAPABILITY_GRANTS rows and authorization.principal_id == command.actor.principal_id (otherwise ValidationRejectedError with InvoiceRejectionCode.ACCESS_DENIED from = models.InvoiceRejectionCode before any read), open one CabinetUnitOfWork through unit_of_work_factory.open and begin it, and inside that single transaction first call begin_effect(self.effect_journal, unit_of_work, BeginEffectRequest(idempotency_key=command.effect_id, operation_kind=authorization.capability, principal_id=authorization.principal_id, actor=command.actor, target_identity=command.project_id, expected_revision=command.expected_revision, request_hash=the imported request_hash(command) — the imported REQUEST_HASH_ALGORITHM constant names that procedure in stored records and is never passed to hashlib)). If reservation.replayed is True and reservation.effect.status == EffectStatus.COMMITTED from = models.EffectStatus it MUST roll back and return ProjectMutationResult(effect_id=command.effect_id, project_revision=reservation.effect.result_revision, artifact_id=None, status=reservation.effect.status, issues=()) without any mutation. The Card commit uses that same unit_of_work; after the Card commit it MUST call commit_effect(self.effect_journal, unit_of_work, command.effect_id, EffectResult(effect_id=command.effect_id, status= EffectStatus.COMMITTED from = models.EffectStatus, result_revision=commit_result.revision, safe_error_code=None, completed_at=the retained clock's now())) and then commit the unit_of_work exactly once; every exception rolls the unit_of_work back before propagating.
save_shopping_list: [PROVENANCE] MUST, after requiring authorization.allowed, authorization.capability present in the imported CAPABILITY_GRANTS rows and authorization.principal_id == command.actor.principal_id (otherwise ValidationRejectedError with InvoiceRejectionCode.ACCESS_DENIED from = models.InvoiceRejectionCode before any read), open one CabinetUnitOfWork through unit_of_work_factory.open and begin it, and inside that single transaction first call begin_effect(self.effect_journal, unit_of_work, BeginEffectRequest(idempotency_key=command.effect_id, operation_kind=authorization.capability, principal_id=authorization.principal_id, actor=command.actor, target_identity=command.proposal.project_revision.card_id, expected_revision=command.proposal.project_revision, request_hash=the imported request_hash(command) — the imported REQUEST_HASH_ALGORITHM constant names that procedure in stored records and is never passed to hashlib)). If reservation.replayed is True and reservation.effect.status == EffectStatus.COMMITTED from = models.EffectStatus it MUST roll back and return ProjectMutationResult(effect_id=command.effect_id, project_revision=reservation.effect.result_revision, artifact_id=None, status=reservation.effect.status, issues=()) without any mutation. The Card commit uses that same unit_of_work; after the Card commit it MUST call commit_effect(self.effect_journal, unit_of_work, command.effect_id, EffectResult(effect_id=command.effect_id, status= EffectStatus.COMMITTED from = models.EffectStatus, result_revision=commit_result.revision, safe_error_code=None, completed_at=the retained clock's now())) and then commit the unit_of_work exactly once; every exception rolls the unit_of_work back before propagating.
store_original_source: [PROVENANCE] MUST, inside the single transaction it opens and after lock_handoff and the secret check, call begin_effect(self.effect_journal, unit_of_work, BeginEffectRequest(idempotency_key=handoff_id, operation_kind= the imported CUSTODY_OPERATION_KIND constant, principal_id=handoff.principal_id, actor=handoff.actor, target_identity=handoff.source_id, expected_revision=handoff.expected_revision, request_hash=the sha256 hex digest of the stored bytes)). If reservation.replayed is True it MUST roll back and raise ValidationRejectedError with InvoiceRejectionCode.ACCESS_DENIED from = models.InvoiceRejectionCode: the handoff was already used once and a used handoff fails closed. After the custody records are written and before the single commit it MUST call commit_effect(self.effect_journal, unit_of_work, handoff_id, EffectResult(effect_id=handoff_id, status= EffectStatus.COMMITTED from = models.EffectStatus, result_revision=handoff.expected_revision, safe_error_code=None, completed_at=the retained clock's now())); every exception rolls the unit_of_work back before propagating.
store_original_source: [SECURITY_BOUNDARY] MUST verify presented_handoff_secret against the locked handoff's secret_verifier with the same timing-safe one-way comparison used for machine credentials and MUST NOT compare plaintext or log either value; a mismatch consumes nothing and raises ValidationRejectedError with InvoiceRejectionCode.ACCESS_DENIED from = models.InvoiceRejectionCode.
release_vps_working_set: [PROVENANCE] MUST, inside the single transaction it opens and after the owner-confirmation and receipt checks, call begin_effect(self.effect_journal, unit_of_work, BeginEffectRequest(idempotency_key=command.confirmation.effect_id, operation_kind=authorization.capability, principal_id=authorization.principal_id, actor=ActorReference(actor_type=ActorType.HUMAN from = models.ActorType, principal_id=authorization.principal_id, channel= the imported CAPABILITY_GRANTS rows channel of the capability, interaction_id=command.confirmation.proposal_id, display_label=None), target_identity=command.working_set_id, expected_revision=command.invoice_revision, request_hash=the imported request_hash(command) — the imported REQUEST_HASH_ALGORITHM constant names that procedure in stored records and is never passed to hashlib)). If reservation.replayed is True and reservation.effect.status == EffectStatus.COMMITTED from = models.EffectStatus it MUST roll back and return VpsWorkingSetReleaseResult(working_set_id=command.working_set_id, released_source_ids=(), preserved_evidence_ids=(command.durable_evidence_id,), status=reservation.effect.status) without a second release. If reservation.replayed is False, or reservation.effect.status != EffectStatus.PREPARED from = models.EffectStatus, or reservation.effect.operation_kind != authorization.capability, it MUST roll back and raise ValidationRejectedError with InvoiceRejectionCode.CONFIRMATION from = models.InvoiceRejectionCode. After the release write and before the single commit it MUST call commit_effect(self.effect_journal, unit_of_work, command.confirmation.effect_id, EffectResult(effect_id=command.confirmation.effect_id, status= EffectStatus.COMMITTED from = models.EffectStatus, result_revision=command.invoice_revision, safe_error_code=None, completed_at=the retained clock's now())); every exception rolls the unit_of_work back before propagating.
issue_upload_handoff: [RETURN_SHAPE] MUST generate the single-use handoff secret from the operating-system CSPRNG, persist only its one-way secret_verifier computed with the same timing-safe verifier scheme as machine credentials, and return UploadHandoffIssuance(handoff=the stored SourceUploadHandoff, handoff_secret=the plaintext) exactly once; the plaintext is never stored, logged, or recoverable.
prepare_chatgpt_proposal: [ORCHESTRATION] MUST call resolve_capability on the retained capability_policy with principal.principal, channel selected from the exact request capability entry in the imported CAPABILITY_GRANTS rows, and request.capability, rejecting an unknown or channel-confused name before any other work; compute proposal_digest as the sha256 hex digest of the canonical JSON of (capability, target_revision, normalized_request); open one CabinetUnitOfWork through unit_of_work_factory.open, begin it, call begin_effect(self.effect_journal, unit_of_work, BeginEffectRequest(idempotency_key=request.interaction_id, operation_kind=request.capability, principal_id=principal.principal.principal_id, actor=principal.actor, target_identity=the target Card id when request.target_revision is present, expected_revision=request.target_revision, request_hash=proposal_digest)) and commit that transaction exactly once, so the proposal is the effect reserved with status EffectStatus.PREPARED from = models.EffectStatus; and return ChatGptProposal(proposal_id=the reserved effect_id, proposal_digest, capability, target_revision, normalized_request, issues, expires_at=the retained clock's now() plus the imported timedelta(seconds=service.settings.chatgpt_proposal_ttl_seconds)).
confirm_chatgpt_effect: [ORCHESTRATION] MUST require request.effect_id == request.proposal_id and request.confirmed, open one CabinetUnitOfWork through unit_of_work_factory.open, begin it, call reconcile_effect(self.effect_journal, unit_of_work, request.effect_id, principal.principal.principal_id), roll the read-only transaction back, and require the reserved effect to have status EffectStatus.PREPARED from = models.EffectStatus, request_hash == request.proposal_digest, operation_kind == request.capability, expected_revision == request.target_revision, and created_at plus the imported timedelta(seconds=service.settings.chatgpt_proposal_ttl_seconds) not earlier than the retained clock's now(); on success it MUST return ConfirmedEffectAuthorization(effect_id, proposal_id, proposal_digest, capability, target_revision, principal_id, expires_at=created_at plus that TTL) and perform no other mutation; any failed requirement raises ConfirmationRequiredError and authorizes nothing.
report_composite_outcome: [ORCHESTRATION] MUST open one CabinetUnitOfWork through unit_of_work_factory.open, begin it, call reconcile_effect(self.effect_journal, unit_of_work, request.effect_id, principal.principal.principal_id), roll the read-only transaction back, and build CompositeOutcome(card_status=the effect status, card_revision=the effect result_revision, source_status and synchronization_status from get_invoice_transfer_status on the retained invoice_exchange for the effect's Invoice target and result revision — source_custody_status and receipt_result or issuance_status, with the imported SYNCHRONIZATION_UNAVAILABLE_STATUS constant when nothing was issued — and the imported SOURCE_NOT_STORED_STATUS constant when the effect has no result revision, safe_codes=the effect and transfer safe codes); it MUST return ChatGptOutcomeReport(interaction_id=request.effect_id, outcome, reported_at=the retained clock's now()) and mutate nothing.
get_invoice_transfer_status: [BEHAVIOR] MUST open and begin one read UoW, list_canonical_admissions_for_invoice and select exact matching Card identity/type/hash/version/status without substituting a newer revision. When exactly one admission matches, load its exact source-set custody. If absent, report SOURCE_NOT_STORED_STATUS without calling get_registered_source_observation with None. If present, obtain the retained observation through get_registered_source_observation and inspect_source_set_custody in the same transaction; report STORED_CUSTODY_STATUS only after all members verify, independently of whether capture proof is still pending. A CanonicalSourceError for unavailable or mismatched custody reports SOURCE_NOT_STORED_STATUS and its bounded code .value string with null issuance and receipt; unrelated canonical-input errors propagate rather than being disguised as missing capture. A pending admission reports the .value string of its first declared pending reason as safe_error_code (safe_error_code is a bounded code string, never an enum member) and no issuance or receipt. A ready admission resolves its exact manifest and list_transfer_issuances_for_manifest; select the latest dated issuance by issued_at then issuance_id, considering undated records only when no dated record exists, load its exact receipt record and project only that receipt. More than one matching source set returns source_custody_status=SOURCE_NOT_STORED_STATUS, null issuance/receipt and CanonicalSourceErrorCode.ADMISSION_AMBIGUOUS.value, requiring exact-set status rather than guessing. With no canonical admission, retain the legacy exact-Card read path through load_card_revision and legacy source custody, without creating transfer work; absent canonical and legacy evidence raises ResourceNotFoundError. Always finally roll back this read transaction; never require a product master row for an imported canonical revision.
get_invoice_transfer_status: [RETURN_SHAPE] MUST report receipt_result accepted or already_accepted only from a stored receipt for that exact manifest hash, and MUST report None for receipt_result when no receipt is stored: transport success is never local durable acceptance.
begin_effect: [DETERMINISM_OR_ORDERING] MUST treat an existing effect with status EffectStatus.PREPARED from = models.EffectStatus whose principal_id, operation_kind, target_identity and expected_revision equal the request as the same reservation and return it with replayed=True without comparing request_hash, so a confirmed proposal and the domain command it authorizes share one effect; request_hash equality is required only against committed or rejected effects, and a difference raises IdempotencyConflictError.
WebGatewayUploadFileBinaryUpload.__init__: [DEPENDENCY_BOUNDARY] MUST retain only the supplied UploadFile and positive max_file_bytes and MUST NOT read, buffer, or inspect its bytes during construction.
WebGatewayUploadFileBinaryUpload.read_chunk: [BEHAVIOR] MUST return at most max_bytes read synchronously from the wrapped upload's underlying file object and an empty bytes value at end of stream, never materializing the whole upload in memory.
load_runtime_settings: [DETERMINISM_OR_ORDERING] MUST deterministically compile only the declared runtime-settings IR and current process environment into one immutable RuntimeSettings value; it MUST apply only declared normalization, defaults, accepted domains, and cross-field constraints, fail closed on every missing required or invalid input, and MUST NOT infer product policy, read notes as data, or create a second settings provider.
prepare_chatgpt_proposal_handler: [ORCHESTRATION] MUST resolve the ChatGPT principal, authorize the exact capability fixed for this handler by http_router_backend, and delegate to prepare_chatgpt_proposal with the chatgpt_interaction service, that principal and the typed proposal request, returning its ChatGptProposal unchanged.
confirm_chatgpt_effect_handler: [ORCHESTRATION] MUST resolve the ChatGPT principal, authorize the exact capability fixed for this handler by http_router_backend, and delegate to confirm_chatgpt_effect with the chatgpt_interaction service, that principal and the typed confirmation request, returning its ConfirmedEffectAuthorization unchanged.
report_composite_outcome_handler: [ORCHESTRATION] MUST resolve the ChatGPT principal, authorize the exact capability fixed for this handler by http_router_backend, and delegate to report_composite_outcome with the chatgpt_interaction service, that principal and the typed outcome request, returning its ChatGptOutcomeReport unchanged.
validate_estimate: [ORCHESTRATION] MUST parse proposal.canonical_json as a JSON object whose sections list holds EstimateSection values, evaluate every row of the imported ESTIMATE_CHECKS table — a row field_path is a dot-separated segment path over the parsed estimate whose asterisk segment iterates the sections list — and record each failed formula as a ValidationIssue(severity= the imported ESTIMATE_VALIDATION_ISSUE_SEVERITY constant, code=the entry code, field_path=the entry field_path, message=the entry formula, expected=the computed value, actual=the supplied value), read the Project currency through get_card_revision on the retained card_workspace with proposal.expected_project_revision and parse ProjectCard from its canonical_json, and return EstimateValidationResult(normalized_estimate=AcceptedEstimateSnapshot(estimate_id=proposal.estimate_id, version=proposal.version, project_id=proposal.project_id, status= the imported ESTIMATE_STATUS_ACCEPTED constant, currency=proposal.currency, sections=the parsed sections, net_total=the sum of section net totals, tax_total=the sum of section tax totals, gross_total=the sum of section gross totals, issued_at=the retained clock's now(), accepted_at=the retained clock's now()) only when no issue exists and None otherwise, net_total, tax_total and gross_total=those sums, issues=the recorded issues, project_revision=proposal.expected_project_revision); it mutates nothing.
validate_estimate: [BEHAVIOR] MUST evaluate each row formula as the same closed call-form vocabulary over the parsed estimate — equals_sum_over aggregates by the asterisk segment over the sections list, equals_expr resolves against the owning section row, non_empty accepts the sections list itself, and equals_project_currency compares the estimate currency to the currency of the Project Card read for the proposal — dispatching on the operation name and never executing formula text as code.
attach_project_estimate: [ORCHESTRATION] MUST, inside the transaction the bracket opens, require command.estimate.issues empty and command.estimate.normalized_estimate present (otherwise ValidationRejectedError with InvoiceRejectionCode.VALIDATION from = models.InvoiceRejectionCode), read the exact Project revision through get_card_revision on the retained card_workspace with command.expected_revision and parse ProjectCard from its canonical_json, derive the successor as project.model_copy(update={accepted_estimate: command.estimate.normalized_estimate}), commit it through commit_card_revision_in_unit_of_work with that unit_of_work and CardRevisionCommitCommand(card_id=command.project_id, card_type= the imported PROJECT_CARD_TYPE constant, expected_content_hash=command.expected_revision.content_hash, canonical_json=successor.model_dump_json(), validation_issues=(), actor=command.actor), and return ProjectMutationResult(effect_id=command.effect_id, project_revision=commit_result.revision, artifact_id=command.estimate.normalized_estimate.estimate_id, status=commit_result.revision.observed_status, issues=()).
save_shopping_list: [ORCHESTRATION] MUST, inside the transaction the bracket opens, read the exact Project revision through get_card_revision on the retained card_workspace with command.proposal.project_revision and parse ProjectCard from its canonical_json, require project.accepted_estimate present with estimate_id == command.proposal.estimate.estimate_id (otherwise ValidationRejectedError with InvoiceRejectionCode.VALIDATION from = models.InvoiceRejectionCode), parse command.proposal.items_json as a JSON list of ShoppingListItem values, set version to one more than the count of project.shopping_lists whose estimate_id equals that estimate_id, build ShoppingListSnapshot(id= the imported SHOPPING_LIST_ID_PREFIX constant followed by command.proposal.project_revision.card_id, the imported SHOPPING_LIST_ID_SEPARATOR constant, the estimate_id, the imported SHOPPING_LIST_ID_SEPARATOR constant and the version, version, project_id=command.proposal.project_revision.card_id, estimate_id, status= the imported SHOPPING_LIST_STATUS_ISSUED constant, currency=project.currency, created_at=the retained clock's now(), source_status=command.proposal.estimate.status, items=the parsed items, net_total=command.proposal.net_total, tax_total=command.proposal.tax_total, gross_total=command.proposal.gross_total), derive the successor as project.model_copy(update={shopping_lists: project.shopping_lists + (snapshot,)}), commit it through commit_card_revision_in_unit_of_work with that unit_of_work and CardRevisionCommitCommand(card_id=command.proposal.project_revision.card_id, card_type= the imported PROJECT_CARD_TYPE constant, expected_content_hash=command.proposal.project_revision.content_hash, canonical_json=successor.model_dump_json(), validation_issues=(), actor=command.actor), and return ProjectMutationResult(effect_id=command.effect_id, project_revision=commit_result.revision, artifact_id=snapshot.id, status=commit_result.revision.observed_status, issues=()).
search_invoices: [SECURITY_BOUNDARY] MUST reject query.limit below 1 or above catalogue.settings.search_max_limit with ValidationRejectedError with InvoiceRejectionCode.VALIDATION from = models.InvoiceRejectionCode before opening a transaction; the caller supplies limit explicitly and catalogue.settings.search_default_limit is the documented client default.
search_provider_cards: [SECURITY_BOUNDARY] MUST reject query.limit below 1 or above workspace.settings.search_max_limit with ValidationRejectedError with InvoiceRejectionCode.VALIDATION from = models.InvoiceRejectionCode before opening a transaction.
list_project_cards: [SECURITY_BOUNDARY] MUST reject query.limit below 1 or above workspace.settings.search_max_limit with ValidationRejectedError with InvoiceRejectionCode.VALIDATION from = models.InvoiceRejectionCode before opening a transaction.
evaluate_readiness: [RETURN_SHAPE] MUST return ReadinessReport(component_statuses=one entry per member of = models.ReadinessComponent in declaration order, each entry the concatenation of that member's .value, the imported COMPONENT_STATUS_SEPARATOR constant and the .value of ComponentReadinessState.READY or ComponentReadinessState.NOT_READY from = models.ComponentReadinessState — always the members' .value strings, never a repr and never any other spelling; safe_error_codes=for each not-ready component in the same order RuntimeSafeErrorCode[that ReadinessComponent member's name] from = models.RuntimeSafeErrorCode; ready=True only when every component is ready; observed_at=request.observed_at); no other status text or code exists.

## Canonical source registration and admission — A19/A20

CanonicalSourceError.__init__: [RETURN_SHAPE] MUST construct a ValueError subclass with the supplied CanonicalSourceErrorCode retained as code and its value as the bounded message. It MUST NOT retain or interpolate original exception messages, bytes, JSON, paths, credentials or filenames.

CanonicalInvoiceSource.__init__: [DEPENDENCY_BOUNDARY] MUST retain only the absolute source_store_root_path and strictly positive max_object_bytes supplied by composition. It performs no source read, environment read, database access, credential lookup, Git command or network request, and does not require installed canonical input for generic startup. All later input paths are confined to the private area selected by the imported CANONICAL_INPUT_DIRECTORY constant under this root.

read_canonical_input_pin: [SECURITY_BOUNDARY] MUST read only the private selector named by the imported CANONICAL_INPUT_PIN_FILENAME constant, bounded by source.max_object_bytes, without following symlinks. Missing input raises CanonicalSourceError with CanonicalSourceErrorCode.INPUT_NOT_CONFIGURED from = models.CanonicalSourceErrorCode. Strict JSON rejects duplicate keys, non-finite numbers, unknown fields and malformed UTF-8; validate as CanonicalInputPin, require a lowercase 64-hex snapshot_sha256 and lowercase 40-hex repository_commit, and return only that exact externally installed pin. Malformed pins raise CanonicalSourceErrorCode.INVALID_PIN. No request or self-reported snapshot field may establish these pins. Open the file through one shared module helper: os.open with the unconditional flags os.O_RDONLY | os.O_NOFOLLOW, a bounded read loop and close in finally; the runtime is POSIX and the flag always exists, so the helper contains no platform probe, no fallback path and no reflective attribute access such as hasattr, which the reflective_attribute_access gate rejects.

read_pinned_canonical_snapshot: [ORCHESTRATION] MUST read only the snapshot selected by pin.snapshot_sha256 below the private canonical input area, walking exactly this order: the directory named by pin.snapshot_sha256, then the directory named by the imported CANONICAL_INPUT_BUNDLE_DIRECTORY constant, then the file named by the imported CANONICAL_INPUT_SNAPSHOT_FILENAME constant. Bound bytes before parsing, require the raw SHA-256 to equal the trusted pin, validate the exact bytes through CanonicalInvoiceSnapshot.model_validate_json in JSON mode with extra fields forbidden (strictness applies to the JSON text: duplicate keys, non-finite numbers and malformed UTF-8 are rejected; a decoded dict is never re-validated in pydantic strict mode, because JSON carries Decimal, date and datetime values as strings), require repository_commit == pin.repository_commit and schema_version == the imported CANONICAL_SNAPSHOT_VERSION constant, and require unique sorted card_id inventory. Verify the raw audit and Invoice schema objects by their declared digests, then call validate_canonical_observation for every observation. A partial or invalid inventory is an error, never a filtered success; this function accepts a retained trusted admission pin as well as a current pin and never reads or replaces the current selector. Open the file through one shared module helper: os.open with the unconditional flags os.O_RDONLY | os.O_NOFOLLOW, a bounded read loop and close in finally; the runtime is POSIX and the flag always exists, so the helper contains no platform probe, no fallback path and no reflective attribute access such as hasattr, which the reflective_attribute_access gate rejects.

load_canonical_invoice_snapshot: [ORCHESTRATION] MUST call read_canonical_input_pin then read_pinned_canonical_snapshot and return its complete verified immutable value. An absent pin or invalid member is explicit failure, never an empty CanonicalInvoiceSnapshot or a guess from an old database queue.

read_canonical_input_object: [PATH_OR_ARTIFACT_POLICY] MUST reject malformed snapshot or object digests before path construction. Use only the private canonical area, walking exactly this order: the directory named by the snapshot digest, then the directory named by the imported CANONICAL_INPUT_BUNDLE_DIRECTORY constant, then the directory named by the imported CANONICAL_INPUT_OBJECTS_DIRECTORY constant, then the file named by the object digest; every opened component must stay within the protected root and no symlink or non-regular final file is accepted. Enforce the configured bound before and during reading, enforce expected_size when provided, and independently verify raw SHA-256. Return only bytes; translate absent, unsafe, oversized or mismatched objects to the corresponding CanonicalSourceErrorCode without leaking a path. It never creates, modifies, deletes or remotely fetches input. Open the file through one shared module helper: os.open with the unconditional flags os.O_RDONLY | os.O_NOFOLLOW, a bounded read loop and close in finally; the runtime is POSIX and the flag always exists, so the helper contains no platform probe, no fallback path and no reflective attribute access such as hasattr, which the reflective_attribute_access gate rejects.

validate_canonical_observation: [VALIDATION_ERROR] MUST reopen card_raw_sha256 through read_canonical_input_object, validate its exact JSON as InvoiceCardV1, and require equality with the embedded observation.card by typed value without rewriting either JSON representation. Require Card id, logical source_id and original canonical content hash to equal the observation and source_set targets. Compute the Card hash from the raw JSON with canonical_json_text_digest and the imported CANONICAL_HASH_PREFIX constant; never hash the decimal-coerced model dump. Require the source-set digest from canonical_source_set_digest with that same prefix to equal source_set_hash. Every member carries its original ordinal, exactly 1 for the first member and increasing by one in file order, a distinct content_id and raw hash, content_id equal to that prefix plus content_sha256, and positive bounded size. Reopen and verify every original. Read the raw capture object when present, classify it independently through classify_legacy_capture and require equality with capture_state; never rewrite a proof or assert document-page completeness. Any target/schema/hash/membership defect raises the corresponding CanonicalSourceErrorCode before downstream custody or admission.

get_canonical_invoice_observation: [ORCHESTRATION] MUST load the current protected pin and reject snapshot_sha256 unequal to it, read that exact pinned snapshot, resolve exactly invoice_id, and require expected_card_content_hash and source_set_hash to match the resolved observation. Unknown Invoice and mismatched revision/set are distinct CanonicalSourceErrorCode refusals. Return the verified existing observation unchanged; never substitute a newer Card, create an association, or query the old product master tables.

read_canonical_original: [SECURITY_BOUNDARY] MUST call get_registered_source_observation with the exact durable record supplied by source_custody, require exactly one member with content_id, then reopen that member through read_canonical_input_object with record.canonical_snapshot_sha256, its raw hash and size. Return only those bounded immutable bytes. The current pin cannot replace retained acceptance; a caller filename, path, hash or request-created custody record is never authority.

read_admitted_card_revision: [PROVENANCE] MUST require admission.status == CanonicalAdmissionState.READY from = models.CanonicalAdmissionState and revision == admission.revision, construct the retained CanonicalInputPin from admission.canonical_snapshot_sha256 and canonical_repository_commit, and validate that exact retained snapshot without substituting the current pin. Resolve its exact Invoice/source set and require its Card hash to match revision. Reopen and decode the original raw Card JSON, then return CanonicalCardRevision(reference=revision, canonical_json=that unchanged JSON text, created_by=admission.actor, created_at=admission.created_at). Those envelope fields record integration observation, not product authorship; Card provenance remains unchanged inside the JSON. Missing retained evidence is explicit failure before issuance.

classify_legacy_capture: [BEHAVIOR] MUST preserve the tested legacy input contract: absent raw bytes classify as LegacyCaptureState.MISSING; malformed strict JSON, non-object evidence, a version other than the exact integer imported LEGACY_CAPTURE_SCHEMA_VERSION constant, or an unequal invoice_id classify INVALID; unequal card_content_hash classifies STALE; unequal source_id classifies MISMATCH; a line_capture_complete value other than True classifies INCOMPLETE; source_line_count and captured_line_count must be positive exact integers, never bools, otherwise INVALID; counts unequal to the exact Card line count classify MISMATCH; only the remaining case classifies VERIFIED_LEGACY_CAPTURE. Use members from = models.LegacyCaptureState. Return the classification only, without changing proof bytes, inferring pages, counting photos as lines or manufacturing confirmation.

canonical_source_set_digest: [DETERMINISM_OR_ORDERING] MUST compute exactly the declared canonical_source_set_digest recipe from = rules.canonical_digest_backend.recipes.canonical_source_set_digest and return its raw digest. It excludes only association_audit_sha256, preserving Card/source/revision and ordered original membership; no behavioral module owns a duplicate hash recipe.

require_protected_operator: [DEPENDENCY_BOUNDARY] MUST delegate to validate_protected_operator through the capability_grants generation unit and return that exact ActorReference. The facade never opens or commits a transaction, accepts a credential string, grants a capability, or invents an operator.

validate_protected_operator: [SECURITY_BOUNDARY] MUST use the supplied active unit_of_work to lock_principal and load_principal for operator.principal.principal_id, require a stored active principal whose kind is PrincipalKind.CABINET_OWNER or PrincipalKind.OPERATOR from = models.PrincipalKind, require equal authenticated principal identity and kind, operator.node is None and operator.actor.principal_id equals that exact principal. Return the unchanged operator.actor. Missing, revoked, machine-bound or mismatched context raises AuthorizationDeniedError before any integration effect. The authenticated operator is supplied by the protected administration boundary; typed caller data alone is not authentication. It performs no independent transaction or credential lifecycle action.

validate_original_payload: [SECURITY_BOUNDARY] MUST require nonempty content within the retained settings.upload_max_file_bytes and identify exactly one accepted format using all segments of the imported CONTENT_FORMAT_SIGNATURES rows. Reject an absent/ambiguous signature or a supplied media type different from the identified media type. Compute exact raw SHA-256 and length and return SourceContentReference with the supplied Card/file identity, computed hash and size, verified media type and display-only filename. Never execute, decompress, recompress, render, concatenate or rewrite an original, and never interpret a filename as a path. This is the common payload check for retained browser upload and canonical registration; it does not allocate a logical source or grant access.

prepare_canonical_source_publications: [PROVENANCE] MUST perform metadata planning only in the supplied caller transaction. For every source_set.files member in order derive publication_id with string_tuple_digest over card_id, source_set_hash and content_sha256; obtain staging_reference_for(publication_id) and final_reference_for(content_sha256) from the byte store. Load the exact existing publication by ID and preserve it only when Card, file content_id, hash, size and both opaque references agree; otherwise refuse the conflict. Insert each absent SourceBytePublication with these exact fields and the imported CANONICAL_PUBLICATION_PENDING_STATUS constant and supplied observed_at timestamps. Return the complete planned tuple. No filesystem write occurs here; the caller must commit plans and the prepared effect before publication begins.

publish_canonical_source_member: [ORCHESTRATION] MUST resolve the exact committed publication plan for the selected Card/set/member and require all immutable fields to agree. Read the original through read_canonical_original, run validate_original_payload with source_id=member.content_id, and compare the result with the accepted file hash, size and media type. If the exact final bytes already verify, reuse them; otherwise reuse a verified planned staging file or stage the exact original and require the returned opaque reference to equal the committed staging reference, then publish to the exact final reference. Reopen through read_stored and verify bytes before updating publication state to the imported CANONICAL_PUBLICATION_PUBLISHED_STATUS constant. Missing metadata is a failure, not permission to create an untracked file. Every failure rolls back the final transaction and leaves the committed plan recoverable; no set success is returned.

verify_canonical_working_members: [VALIDATION_ERROR] MUST require record.card_id and logical source_id to equal the canonical observation, exact source_set_hash, stored custody status, and ordered record.contents equal to the SourceContentReference projection of the accepted members: for the member with ordinal i, contents[i-1] has card_id == record.card_id, source_id == member.content_id, content_hash == member.content_sha256 (the bare lowercase 64-hex digest, exactly what final_reference_for accepts, never prefixed with CANONICAL_HASH_PREFIX), size_bytes == member.size_bytes, media_type == member.media_type and display_filename == member.display_filename. Reopen every required member through source_byte_store.read_stored using final_reference_for, exact hash and size. Missing, released, mismatched or corrupt evidence raises CanonicalSourceError with the appropriate CanonicalSourceErrorCode; it cannot be replaced by the legacy singleton record, a successful upload response or a hash declaration.

inspect_source_set_custody: [ORCHESTRATION] MUST lock_source_set_custody and load_source_set_custody for the exact Card/set in the caller transaction. Return None when no record exists; otherwise call verify_canonical_working_members and return the exact verified record. It never opens a second unit of work, commits, repairs missing bytes, or silently turns failed verification into stored custody.

register_canonical_source_set: [SECURITY_BOUNDARY] MUST authenticate the protected operator through require_protected_operator inside each operation transaction and require command.actor to equal the returned actor. Reserve the exact command with begin_effect using the imported CANONICAL_REGISTRATION_OPERATION, command.effect_id, actor/principal, target invoice_id, expected_revision=None and request_hash(command). Explicitly compare the reserved request_hash, operation, principal and target even while PREPARED; the legacy journal proposal exception is not permitted for this operator operation. Conflicting input raises IdempotencyConflictError before any file write. Never call confirmation, Card commit, metadata attachment, product seed, or proof mutation.

register_canonical_source_set: [ORCHESTRATION] MUST first check any committed M192 result under the exact effect and return its registration_result with only replayed=True, after authority and request equality checks, without refreshing custody or requiring the current input pin. For an existing PREPARED effect with its committed pending trust anchor, resolve and verify the exact command Card/hash/set through get_registered_source_observation instead of the current pin; never replace the recorded request hash. For a new preparation resolve through get_canonical_invoice_observation. Reject empty membership, lock the exact set, insert an absent pending M185 record retaining the verified input pins (canonical_snapshot_sha256 and canonical_repository_commit copied from the current protected pin returned by read_canonical_input_pin(custody.canonical_source), whose snapshot_sha256 get_canonical_invoice_observation already required to equal command.canonical_snapshot_sha256; neither value is derived from any observation or audit digest) and declared ordered file metadata with all custody timestamps null, preserve equivalent existing evidence, and commit this trust anchor and the prepared effect with prepare_canonical_source_publications before filesystem writes. Open the final transaction, recheck operator and exact effect, lock the exact source set and recheck any completed result, then obtain the observation through get_registered_source_observation from the durable trust anchor and call publish_canonical_source_member for every planned member in order, verifying each before completing the set. Upgrade only the exact pending SourceSetCustodyRecord to stored with its ordered verified contents, accepted_at from the retained clock and null rejection/release fields while preserving logical source, immutable input pins and actor; an equal existing stored record is preserved after re-verification. A released record is never silently revived by replay. Atomically insert CanonicalIntegrationEffectRecord with the exact non-replayed registration result, commit_effect with result_revision=None and commit once. Roll back each failed transaction; prepared plans remain durable and final partial work never becomes complete custody.

register_canonical_source_set: [RETURN_SHAPE] MUST return exactly SourceSetRegistrationResult for the committed full set. A committed journal effect without its matching typed M192 result is an integrity error, never a reconstructed current result. Prepared recovery uses the same logical effect and exact file plans; it cannot allocate a replacement effect identity or overwrite immutable results.

retrieve_source_set_file: [SECURITY_BOUNDARY] MUST require authorization.allowed, authorization.capability equal to the imported CANONICAL_PACKAGE_READ_CAPABILITY, a nonempty principal_id and a non-null entity_scope for the exact Card/revision, then open one read transaction, load the exact source-set custody record and require its logical source_id to equal request.source_id, its exact source_set_hash and stored status, and one content member matching request.content_hash. Load the exact canonical admission for this Card/set in the same transaction and require READY plus authorization.entity_scope.revision equal to its exact revision. This is internal package-read authority; it does not grant a generic source-download capability. Read that member through the byte store with its exact hash and size and discard the verification buffer before returning CanonicalSourceFileDownload whose ContentReference.content_id is the member.source_id, preserving the per-file wire identity. Missing membership, released bytes or corruption is explicit failure. Finally roll back the read transaction; no path, credential, arbitrary first photograph or unverified stream is returned.

evaluate_canonical_admission: [RETURN_SHAPE] MUST return a deterministic tuple of CanonicalAdmissionPendingReason values: first INVOICE_NOT_CONFIRMED when the exact Card status is not confirmed; then SOURCE_MEMBERSHIP_MISSING when the accepted set is empty; then CUSTODY_NOT_VERIFIED when no matching verified custody record is supplied; finally the capture reason selected by the non-verified LegacyCaptureState whose value equals the imported CAPTURE_PENDING_REASON_PREFIX plus capture_state.value. Only an empty tuple is ready. Confirmed status alone, a file count and custody success never create a capture proof.

derive_canonical_transfer_records: [FIELD_PROJECTION] MUST use exactly one InvoiceCardRevisionReference from the unchanged observation Card and supplied revision. Project every original, in accepted ordinal order, to ContentReference using member.content_id, content_sha256, size_bytes, media_type and display_filename; separately project working-set SourceContentReference with card_id, source_id=that file content_id and content_hash=that file content_sha256 (bare, never prefixed). Use the accepted MANIFEST_VERSION and supplied observed_at as generated_at, compute manifest_hash by the existing canonical recipe excluding manifest_id/hash, and derive both manifest and working-set IDs with their accepted prefixes and that hash. Return the immutable manifest and an available InvoiceWorkingSetRecord with the exact revision and ordered required_sources, created_at=observed_at and released_at=None; neither Card/source identity nor wire structure changes.

admit_canonical_invoice: [SECURITY_BOUNDARY] MUST open one operation transaction, call require_protected_operator, require the exact command actor, and reserve the command through begin_effect using the imported CANONICAL_ADMISSION_OPERATION, command.effect_id, exact principal/target, expected_revision=None and request_hash(command). Explicitly reject a different reserved request hash even for PREPARED. For a committed effect, return only its retained M192 admission_result with replayed=True after exact request/actor checks; absence of that result is an integrity error. A new admission does not call confirm_invoice, commit_card_revision, a product master insert or any capture-writing operation.

admit_canonical_invoice: [ORCHESTRATION] MUST resolve the exact trusted canonical observation through get_canonical_invoice_observation, lock_canonical_invoice_admission for invoice_id/source_set_hash and load it. Preserve the first observation actor, created_at and exact revision reference; a READY record, manifest and receipts remain immutable. Inspect the exact source-set custody in this same transaction; custody-unavailable or custody-mismatch evidence is a pending prerequisite, while unrelated input/authority errors propagate. Derive pending reasons through evaluate_canonical_admission. For a new or pending admission, retain the current trusted pin (canonical_snapshot_sha256 and canonical_repository_commit copied from the current protected pin returned by read_canonical_input_pin(exchange.canonical_source), whose snapshot_sha256 get_canonical_invoice_observation already required to equal command.canonical_snapshot_sha256; neither value is derived from any observation or audit digest), the capture raw hash, exact reasons and updated_at; an empty reason tuple produces READY and its exact manifest/work record from derive_canonical_transfer_records using the first admission observation time. Existing manifest/work IDs must match by value or the operation refuses conflict. Insert/update operational admission, insert absent manifest/work records, store the exact M192 admission result and commit_effect with result_revision=None atomically, then commit once. Roll back all operational writes on failure; the external canonical repository is never a transaction participant.

get_canonical_invoice_admission: [ORCHESTRATION] MUST open and begin a read transaction, call require_protected_operator, load the exact invoice_id/source_set_hash admission and return that stored record or None only if never observed, then finally roll back. A pending record must expose its exact reasons; an empty transfer queue cannot replace this result and no newer revision is substituted.

read_manifest_admission: [VALIDATION_ERROR] MUST load_canonical_admission_for_manifest in the supplied transaction and require an exact READY record, matching manifest ID/hash, Invoice identity, and the single manifest Card hash/status/version matching its retained revision. Missing or mismatched admitted evidence fails before issuance; no old product table or newly selected current pin is a substitute.

SourceByteStore.staging_reference_for: [DEPENDENCY_BOUNDARY] MUST expose deterministic confined staging-reference derivation for the exact publication_id before bytes are written, returning no physical path and performing no I/O. This reference is part of the committed publication plan and the later stage operation must return this same opaque reference.

LocalFilesystemSourceByteStore.staging_reference_for: [PATH_OR_ARTIFACT_POLICY] MUST validate publication_id by the same safe token constraint as stage and derive the exact opaque staging reference that stage will use under the retained private store. It creates nothing, follows no caller path and never returns a filesystem path.

LocalFilesystemSourceByteStore.stage: [PROVENANCE] MUST derive its candidate reference only through staging_reference_for(publication_id) and return that same reference after successful exclusive write and verification. A previously interrupted candidate is inspected through the committed publication plan by its owner, never blindly overwritten or mistaken for completed custody.

CabinetUnitOfWork.lock_source_set_custody: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.

CabinetUnitOfWork.load_source_set_custody: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.

CabinetUnitOfWork.insert_source_set_custody: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.

CabinetUnitOfWork.update_source_set_custody: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.

CabinetUnitOfWork.lock_canonical_invoice_admission: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.

CabinetUnitOfWork.load_canonical_invoice_admission: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.

CabinetUnitOfWork.insert_canonical_invoice_admission: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.

CabinetUnitOfWork.update_canonical_invoice_admission: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.

CabinetUnitOfWork.load_canonical_admission_for_manifest: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.

CabinetUnitOfWork.lock_canonical_integration_effect_result: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.

CabinetUnitOfWork.load_canonical_integration_effect_result: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.

CabinetUnitOfWork.insert_canonical_integration_effect_result: [PROVENANCE] MUST perform exactly the typed operational lock/read/insert/update declared for this method in the persistence backend inside the caller-owned active transaction. Preserve exact identity and stored values, return absence only for an absent exact record, never substitute a later revision, apply readiness policy, open another transaction, or commit independently.


create_cabinet_web_app: [DEPENDENCY_BOUNDARY] MUST construct exactly one CanonicalInvoiceSource from the same typed settings.source_store_root_path and settings.upload_max_file_bytes and inject it into SourceCustodyService and InvoiceExchange; pass the shared EffectJournal into InvoiceExchange. No new environment provider or canonical-input prerequisite is added to generic startup. The protected operator installs source pins separately; missing input is reported by canonical operations. Import the concrete source class only from canonical_invoice_source.

CanonicalSourceFileDownload.__init__: [DEPENDENCY_BOUNDARY] MUST retain the exact ContentReference and SourceByteStore, initialize an unread cursor and no payload, and perform no byte read. Constructing all download descriptors for a manifest never buffers all original files. The descriptor carries no path, credential or inferred identity.
CanonicalSourceFileDownload.describe: [RETURN_SHAPE] MUST return the retained exact ContentReference unchanged and perform no I/O or mutation.
CanonicalSourceFileDownload.read_chunk: [BEHAVIOR] MUST reject non-positive max_bytes. On the first read only, obtain this exact bounded file through source_byte_store.read_stored with final_reference_for(content.content_hash), exact hash and size; mismatch fails before returning bytes. Return the next at-most-max_bytes slice, advance the cursor, and discard the retained payload when its final bytes have been returned. Subsequent EOF reads return empty bytes without reopening the file. Verification and package construction MUST NOT retain whole-file buffers across other manifest members; the package stream reads one source at a time in manifest order. Post-issuance byte loss or corruption remains an explicit transfer interruption, never an empty successful file.

get_registered_source_observation: [PROVENANCE] MUST construct the retained CanonicalInputPin from record.canonical_snapshot_sha256 and canonical_repository_commit, call read_pinned_canonical_snapshot, resolve exactly record.card_id and source_set_hash, require record.source_id == observation.source_set.source_id, and require record.contents to equal, in order and in length, the SourceContentReference projection of the accepted members: for the member with ordinal i, contents[i-1] has card_id == record.card_id, source_id == member.content_id, content_hash == member.content_sha256 (the bare lowercase 64-hex digest, exactly what final_reference_for accepts, never prefixed with CANONICAL_HASH_PREFIX), size_bytes == member.size_bytes, media_type == member.media_type and display_filename == member.display_filename. Return that exact observation without consulting the current selector or changing custody status; the caller must obtain the record from its trusted operational UoW, never request data.

release_vps_working_set: [SECURITY_BOUNDARY] MUST, after the existing owner authorization and exact confirmation checks, resolve the exact stored working set and manifest before using any release evidence. For a canonical A20 admission, the current VpsWorkingSetReleaseCommand does not carry a typed local verification with required_source_ids and verified_source_ids; its hash tuples and a nonempty durable_evidence_id cannot fabricate that missing A10 evidence. Refuse release by rolling back the read transaction and returning VpsWorkingSetReleaseResult with command.working_set_id, released_source_ids=(), preserved_evidence_ids=(manifest.manifest_id,), and status=CanonicalSourceErrorCode.DURABLE_VERIFICATION_MISSING.value from = models.CanonicalSourceErrorCode; preserve all bytes and records and do not reserve or commit a release effect. Registration, admission, an accepted transfer receipt alone, or an old singleton custody row never bypasses this refusal. A later release-contract extension must supply and verify the complete exact local evidence before enabling canonical-set deletion.

CanonicalInvoiceSource.__init__: [FIELD_ASSIGNMENT] MUST store the supplied source_store_root_path and max_object_bytes in the same-named instance fields; no alternate settings object or hidden origin selector is created.
SourceCustodyService.__init__: [FIELD_ASSIGNMENT] MUST retain unit_of_work_factory, source_byte_store, effect_journal, clock, settings and canonical_source in same-named instance fields. Helpers use these exact declared dependencies; no independent connections, stores or clocks are created.
InvoiceExchange.__init__: [FIELD_ASSIGNMENT] MUST retain unit_of_work_factory, card_workspace, source_custody, clock, canonical_source and effect_journal in same-named instance fields, preserving their exact supplied identity.
CanonicalSourceFileDownload.__init__: [FIELD_ASSIGNMENT] MUST retain content and source_byte_store in same-named instance fields and initialize payload=None, offset=0 and finished=False. The internal cursor state owns only this download; no file bytes enter module-global or shared service state.
CabinetUnitOfWork.list_canonical_admissions_for_invoice: [PROVENANCE] MUST return the complete exact operational rows selected by the declared typed filter in the caller transaction, in the deterministic persistence order, without applying admission policy, selecting a newer revision, opening another UoW or committing.
CabinetUnitOfWork.list_transfer_issuances_for_manifest: [PROVENANCE] MUST return the complete exact operational rows selected by the declared typed filter in the caller transaction, in the deterministic persistence order, without applying admission policy, selecting a newer revision, opening another UoW or committing.

pull_invoice_package: [RETURN_SHAPE] MUST preserve the existing wire framing: encode InvoicePackageMetadata as UTF-8 JSON, prefix its byte length with exactly PACKAGE_METADATA_LENGTH_PREFIX_BYTES bytes in unsigned big-endian order, then stream that metadata and each original in manifest order. The stream never includes canonical input wrappers or filesystem locations.
derive_canonical_transfer_records: [PROVENANCE] MUST use the imported MANIFEST_HASH_ALGORITHM as the recorded name of the existing manifest_hash procedure, never as a hashlib algorithm argument or a replacement recipe.
evaluate_validation_checks: [BEHAVIOR] MUST evaluate invoice with the retained deterministic validation_rules backend declared by 70_validation_rules_closure.json. Evaluate the imported INVOICE_VALIDATION_CHECKS rows using their exact code, field_path and formula under that closed backend; never interpret arbitrary expressions or omit a declared check.
evaluate_validation_checks: [RETURN_SHAPE] MUST return the complete ordered tuple of ValidationIssue values produced from the imported INVOICE_VALIDATION_CHECKS, with message from the imported VALIDATION_ISSUE_MESSAGE, severity from the imported VALIDATION_ISSUE_SEVERITY and order from the imported VALIDATION_ISSUE_ORDER followed by numeric line index; present offending values populate actual as text and absent values populate actual as None.
