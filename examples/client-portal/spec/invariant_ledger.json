{
  "schema_version": 1,
  "invariants": [
    {"id": "INV-001", "statement": "every project-scoped operation passes the guard sequence: valid session, active principal (verified email for staff), project access, required capability, Registry lifecycle policy; first failure wins", "owner_function": "_authorize_project_operation", "landing": {"kind": "note", "text": "_authorize_project_operation: [ORCHESTRATION] MUST execute = rules.project_guard_sequence in order with first failure winning; it MUST resolve persisted revocation/current-principal state, ignore client Registry context, validate the same UUID server-side, and reject inactive mutation targets."}},
    {"id": "INV-002", "statement": "roles and capabilities are never read from request data; capability sets are static rules per principal type", "owner_function": "_require_capability", "landing": {"kind": "rules", "path": "capability_catalog"}},
    {"id": "INV-003", "statement": "passwords, session tokens, viewer access tokens, service credentials, and challenge tokens are stored only in verification/hash form and never logged", "owner_function": "issue_verification_secret", "landing": {"kind": "note", "text": "issue_verification_secret: [RULE_REFERENCE] MUST implement = rules.verification_envelope_policy using entropy_bytes of cryptographically secure randomness and return plaintext once plus a salted verification_hash."}},
    {"id": "INV-004", "statement": "authentication and challenge flows never reveal account or viewer existence; auth failures never expose project data", "owner_function": "conceal_authentication_failure", "landing": {"kind": "note", "text": "conceal_authentication_failure: [SECURITY_BOUNDARY] MUST return a fresh parameterless AuthenticationRejected carrying no existence or project evidence."}},
    {"id": "INV-005", "statement": "a revoked grant, credential, viewer, or session blocks the next authorization check immediately, per the revocation cascade table", "owner_function": "_authorize_project_operation", "landing": {"kind": "rules", "path": "revocation_cascades"}},
    {"id": "INV-006", "statement": "a suspended or disabled staff account and a disabled service principal have no valid sessions or usable credentials", "owner_function": "_authorize_project_operation", "landing": {"kind": "rules", "path": "revocation_cascades"}},
    {"id": "INV-007", "statement": "viewer capabilities are read-only; a viewer can never mutate portal data or access management", "owner_function": "_require_capability", "landing": {"kind": "rules", "path": "principal_capability_sets"}},
    {"id": "INV-008", "statement": "at most one non-revoked ViewerAccessCredential per viewer; re-issue revokes the previous credential atomically", "owner_function": "issue_viewer_access", "landing": {"kind": "note", "text": "issue_viewer_access: [ORCHESTRATION] MUST revoke every non-revoked prior credential, persist exactly one new verification_hash, revoke active sessions, append viewer.credential_issue, and return plaintext only in ViewerCredentialIssue."}},
    {"id": "INV-009", "statement": "at most one active grant per (viewer_id, project_id) and per (service_principal_id, project_id)", "owner_function": "PortalTransaction.commit", "landing": {"kind": "note", "text": "PortalTransaction.commit: [ORCHESTRATION] MUST atomically persist the complete write set or none, enforce at most one active viewer and producer grant per respective pair, preserve exact expense allocation sums and section reference restrictions, and forbid update/delete of AuditRecord, SnapshotImportRecord, and ApprovedEstimateSnapshot."}},
    {"id": "INV-010", "statement": "challenge tokens are purpose-bound, single-use, time-limited, revocable; replay and cross-flow use are rejected", "owner_function": "_issue_challenge", "landing": {"kind": "rules", "path": "email_challenge_policy"}},
    {"id": "INV-011", "statement": "successful password reset revokes all active staff sessions and all outstanding reset tokens of the account and sends a password-changed notice", "owner_function": "complete_password_reset", "landing": {"kind": "note", "text": "complete_password_reset: [ORCHESTRATION] MUST consume only the matching reset token, enforce R16, replace password_hash, revoke every active session and outstanding reset token atomically, append staff.password_reset_complete, then send password_changed_notice after commit."}},
    {"id": "INV-012", "statement": "a new email becomes the login identifier only when its PendingEmailChange is consumed; the previous address is notified after completion", "owner_function": "confirm_email_change", "landing": {"kind": "note", "text": "confirm_email_change: [ORCHESTRATION] MUST consume only the matching email-change token, atomically replace the login email and append staff.email_changed, then notify the previous address after commit."}},
    {"id": "INV-013", "statement": "email delivery success or failure never confirms address ownership or affects challenge validity", "owner_function": "deliver_email_message", "landing": {"kind": "note", "text": "deliver_email_message: [ORCHESTRATION] MUST persist the delivery attempt/outcome around the provider call, translate provider failure to EmailDeliveryError, and MUST NOT mark any challenge valid, consumed, or verified."}},
    {"id": "INV-014", "statement": "a staff account authenticates only in active status with a verified email; pending_verification cannot log in", "owner_function": "authenticate_staff", "landing": {"kind": "note", "text": "authenticate_staff: [SECURITY_BOUNDARY] MUST normalize email, accept only an active verified account with a matching password, issue a hashed fixed-expiry session, audit success or concealed failure, and never reveal which check failed."}},
    {"id": "INV-015", "statement": "administrative recovery override is an exceptional flow with enhanced audit, never the primary email-change mechanism", "owner_function": "perform_recovery_override", "landing": {"kind": "note", "text": "perform_recovery_override: [SECURITY_BOUNDARY] MUST be administrator-only, enforce R16 on replacement_email, append enhanced staff.recovery_override audit atomically with the replacement, and MUST NOT serve as the normal email-change path."}},
    {"id": "INV-016", "statement": "issuing a new challenge of one purpose revokes outstanding challenges of that purpose for the account; issuing is rate-limited per account", "owner_function": "_issue_challenge", "landing": {"kind": "rules", "path": "email_challenge_policy"}},
    {"id": "INV-017", "statement": "an operator may access a project only through one active StaffProjectAssignment; revocation blocks the next project-scoped authorization check immediately; current administrators derive project scope from the single administrative area policy", "owner_function": "_require_staff_scope", "landing": {"kind": "note", "text": "_require_staff_scope: [SECURITY_BOUNDARY] MUST accept administrators only under the single-area policy and operators only with an active StaffProjectAssignment for the same project_id."}},
    {"id": "INV-018", "statement": "staff login email uniqueness and lookup use the R16 trimmed syntax-valid Unicode-casefolded form without provider-specific rewriting", "owner_function": "_normalize_staff_email", "landing": {"kind": "rules", "path": "staff_identity_input_policy"}},
    {"id": "INV-019", "statement": "new staff passwords satisfy R16 length/control-character policy and are hashed exactly as supplied without trimming or normalization", "owner_function": "_validate_new_password", "landing": {"kind": "rules", "path": "staff_identity_input_policy"}},
    {"id": "PLAT-REG-001", "statement": "Registry is the only creator and current owner of Registry project_id; the portal never generates one", "owner_function": "validate_project_reference", "landing": {"kind": "note", "text": "validate_project_reference: [PROVENANCE] MUST resolve the Registry endpoint from the environment variable named by = config.registry_endpoint_env_name and use = config.registry_timeout_seconds; it MUST never create a project_id or access Registry storage directly."}},
    {"id": "PLAT-REG-002", "statement": "portal identity linkage uses the returned UUID, never display name or address", "owner_function": "_validate_reference_identity", "landing": {"kind": "property", "expression": "result.project_id == requested_id"}},
    {"id": "PLAT-REG-003", "statement": "a linked mutation succeeds only after server-side typed validation reports the same UUID as active", "owner_function": "_authorize_project_operation", "landing": {"kind": "note", "text": "_authorize_project_operation: [ORCHESTRATION] MUST execute = rules.project_guard_sequence in order with first failure winning; it MUST resolve persisted revocation/current-principal state, ignore client Registry context, validate the same UUID server-side, and reject inactive mutation targets."}},
    {"id": "PLAT-REG-004", "statement": "client-supplied Registry context is never authoritative", "owner_function": "_authorize_project_operation", "landing": {"kind": "note", "text": "_authorize_project_operation: [ORCHESTRATION] MUST execute = rules.project_guard_sequence in order with first failure winning; it MUST resolve persisted revocation/current-principal state, ignore client Registry context, validate the same UUID server-side, and reject inactive mutation targets."}},
    {"id": "PLAT-REG-005", "statement": "Registry failures translate through the closed RegistryFailureKind table; no silent fallback to cached, client-supplied, or fabricated context", "owner_function": "_translate_registry_failure", "landing": {"kind": "note", "text": "_translate_registry_failure: [RULE_REFERENCE] MUST map the closed failure through = rules.registry_failure_translation and MUST NOT fall back to cached, client-supplied, or fabricated context."}},
    {"id": "PLAT-REG-006", "statement": "the portal stores no Registry snapshot as current truth; live context is fetched through the gateway; archived context stays readable but grants no mutation permission", "owner_function": "get_project_context", "landing": {"kind": "note", "text": "get_project_context: [PROVENANCE] MUST fetch live typed Registry context, accept active or archived status for reads, validate response identity, and MUST NOT persist or treat it as portal-owned current truth."}},
    {"id": "PLAT-REG-007", "statement": "portal-owned client data (viewer display name) remains independent of Registry customer_ref", "owner_function": "create_viewer", "landing": {"kind": "note", "text": "create_viewer: [PROVENANCE] MUST create portal-owned display_name independently of Registry customer_ref and append viewer.create atomically after access-management authorization."}},
    {"id": "INV-020", "statement": "an accepted ApprovedEstimateSnapshot is immutable; versions are append-only and never overwritten or deleted", "owner_function": "import_snapshot", "landing": {"kind": "note", "text": "import_snapshot: [ORCHESTRATION] MUST authorize producer scope and active Registry identity before validating content; equal replay returns the existing import result, conflict stores no snapshot, and every accepted/rejected catalog outcome appends one immutable import record plus audit atomically."}},
    {"id": "INV-021", "statement": "re-publication with identical (project, estimate_id, estimate_version) and identical fingerprint is an idempotent replay; a different fingerprint for the same identity is an integrity conflict storing nothing", "owner_function": "_classify_snapshot_replay", "landing": {"kind": "rules", "path": "idempotency_identities"}},
    {"id": "INV-022", "statement": "import and activation are distinct actions; a producer holds no activation capability and never activates its own import", "owner_function": "activate_budget_version", "landing": {"kind": "note", "text": "activate_budget_version: [ORCHESTRATION] MUST validate version ownership and unresolved-allocation policy, atomically deactivate the prior active version, activate exactly the requested version, and append snapshot.activate without granting producers this operation."}},
    {"id": "INV-023", "statement": "at most one active BudgetVersion per project; activation atomically deactivates the previous active version", "owner_function": "activate_budget_version", "landing": {"kind": "note", "text": "activate_budget_version: [ORCHESTRATION] MUST validate version ownership and unresolved-allocation policy, atomically deactivate the prior active version, activate exactly the requested version, and append snapshot.activate without granting producers this operation."}},
    {"id": "INV-024", "statement": "at most one manual BudgetVersion per project; manual section plans are mutable only while the project is active; imported plans are immutable copies of their snapshot", "owner_function": "upsert_manual_section_plan", "landing": {"kind": "note", "text": "upsert_manual_section_plan: [ORCHESTRATION] MUST require an active project and the single manual version, create/rename the stable PortalSection when needed, validate R9 amounts, upsert its plan, and append budget.section_plan_edit atomically without changing imported plans."}},
    {"id": "INV-025", "statement": "PortalSection identity is stable across versions and renames; automatic linking uses source_key equality only; display-name matching is forbidden", "owner_function": "_link_snapshot_sections", "landing": {"kind": "note", "text": "_link_snapshot_sections: [RULE_REFERENCE] MUST reuse sections only by exact source_key, create stable new identities otherwise, preserve existing identities across rename, and MUST NOT match display_name."}},
    {"id": "INV-026", "statement": "planned budget values never derive from actual expenses; mutable PresuPro estimate state never changes the portal budget outside the snapshot import boundary", "owner_function": "import_snapshot", "landing": {"kind": "note", "text": "import_snapshot: [ORCHESTRATION] MUST authorize producer scope and active Registry identity before validating content; equal replay returns the existing import result, conflict stores no snapshot, and every accepted/rejected catalog outcome appends one immutable import record plus audit atomically."}},
    {"id": "INV-027", "statement": "budget totals and all financial aggregates are derived at read time, never stored as editable values", "owner_function": "build_budget_view", "landing": {"kind": "note", "text": "build_budget_view: [PROVENANCE] MUST derive all section and total fields at read time from the selected/active BudgetVersion, plans, included expenses, and progress; it MUST NOT persist totals."}},
    {"id": "INV-028", "statement": "a PortalSection referenced by any allocation or progress record is never deleted", "owner_function": "PortalTransaction.commit", "landing": {"kind": "note", "text": "PortalTransaction.commit: [ORCHESTRATION] MUST atomically persist the complete write set or none, enforce at most one active viewer and producer grant per respective pair, preserve exact expense allocation sums and section reference restrictions, and forbid update/delete of AuditRecord, SnapshotImportRecord, and ApprovedEstimateSnapshot."}},
    {"id": "INV-029", "statement": "a producer import is authorized for the target project per scope_mode and grants before content validation", "owner_function": "authorize_snapshot_import", "landing": {"kind": "note", "text": "authorize_snapshot_import: [ORCHESTRATION] MUST enforce producer scope before publication validation and delegate active Registry lifecycle and capability checks to _authorize_project_operation."}},
    {"id": "INV-030", "statement": "one recognized_document_id creates at most one Expense and one ExpenseDocument; equal intake_fingerprint replay returns the existing records, while a different fingerprint is an integrity conflict changing nothing", "owner_function": "create_expense_from_recognition", "landing": {"kind": "note", "text": "create_expense_from_recognition: [ORCHESTRATION] MUST accept only confirmed complete supported-version normalized input, validate explicit allocations, apply R8 replay before mutation, and atomically insert one Expense plus one ExpenseDocument plus allocations and expense.create audit."}},
    {"id": "INV-031", "statement": "an Expense is created only from a confirmed, complete normalized result with a supported contract version; confidence, provider fields, and raw responses are never persisted", "owner_function": "_project_expense_intake", "landing": {"kind": "note", "text": "_project_expense_intake: [FIELD_PROJECTION] MUST project only confirmed normalized business fields, recognized_document_id, intake_fingerprint inputs, file_ref, and explicit allocations; it MUST NOT project confidence, provider fields, or raw responses."}},
    {"id": "INV-032", "statement": "the sum of an expense's allocations equals its total_amount at every commit point; amount correction and allocation adjustment are one atomic operation", "owner_function": "PortalTransaction.commit", "landing": {"kind": "note", "text": "PortalTransaction.commit: [ORCHESTRATION] MUST atomically persist the complete write set or none, enforce at most one active viewer and producer grant per respective pair, preserve exact expense allocation sums and section reference restrictions, and forbid update/delete of AuditRecord, SnapshotImportRecord, and ApprovedEstimateSnapshot."}},
    {"id": "INV-033", "statement": "automatic proportional allocation is forbidden; every allocation amount is an explicit operator value", "owner_function": "_build_replacement_allocations", "landing": {"kind": "note", "text": "_build_replacement_allocations: [FORBIDDEN_ACTION] MUST create exactly one allocation per instruction with supplied amount/target, expense project provenance, actor_id, and changed_at; MUST NOT proportion, split, or invent a remainder."}},
    {"id": "INV-034", "statement": "exactly one ExpenseDocument per Expense regardless of allocation count; the binary stays outside the record", "owner_function": "_build_expense_aggregate", "landing": {"kind": "note", "text": "_build_expense_aggregate: [FIELD_ASSIGNMENT] MUST create exactly one ExpenseDocument for the new Expense, keep binary content outside records behind intake.file_ref, and create one allocation per explicit instruction."}},
    {"id": "INV-035", "statement": "only confirmed expenses with included=true participate in financial totals; excluded expenses remain visible", "owner_function": "_project_expense_summary", "landing": {"kind": "note", "text": "_project_expense_summary: [BEHAVIOR] MUST include only included=true amounts in included_total/other_expenses_total, keep excluded expenses in result.expenses and excluded_total, and never persist totals."}},
    {"id": "INV-036", "statement": "allocation targets and progress sections belong to the expense's/project's own project; cross-project references are rejected", "owner_function": "validate_project_section", "landing": {"kind": "note", "text": "validate_project_section: [SECURITY_BOUNDARY] MUST return only a section whose project_id equals project_id and raise BudgetValidationError for missing or cross-project identity without revealing another project."}},
    {"id": "INV-037", "statement": "all monetary values are EUR with decimal 2-digit semantics and ROUND_HALF_UP; foreign currency input is rejected explicitly, never converted", "owner_function": "validate_eur_amount", "landing": {"kind": "rules", "path": "money_policy"}},
    {"id": "INV-040", "statement": "completion percent is a manual staff value in [0,100]; photographs never set progress", "owner_function": "set_section_progress", "landing": {"kind": "note", "text": "set_section_progress: [BEHAVIOR] MUST validate the project section and integer range 0..100, upsert the manual value, and append progress.update atomically; photographs MUST NOT alter it."}},
    {"id": "INV-041", "statement": "completed work value and overall progress follow the R9 formulas; overall progress is unavailable, never guessed, when no planned work cost exists", "owner_function": "calculate_overall_progress", "landing": {"kind": "rules", "path": "money_policy"}},
    {"id": "INV-042", "statement": "a WorkPayment is idempotent by payment_id and is never a factura or accounting posting; payments total is derived", "owner_function": "register_work_payment", "landing": {"kind": "note", "text": "register_work_payment: [RULE_REFERENCE] MUST enforce EUR/R9 and = rules.idempotency_identities; equal payment_id/content returns existing, conflict changes nothing, and a new payment plus payment.register audit commits atomically without creating an invoice/accounting posting."}},
    {"id": "INV-043", "statement": "a ProgressPhoto is idempotent by intake_id; the gallery is chronological; visibility changes never alter financial or progress data", "owner_function": "publish_progress_photo", "landing": {"kind": "note", "text": "publish_progress_photo: [ORCHESTRATION] MUST apply intake_id replay, validate optional section project scope, persist one file_ref-backed photo, and append photo.publish atomically; it MUST NOT set progress or financial values."}},
    {"id": "INV-044", "statement": "remaining materials show overspend as negative values, never clamped or hidden; missing data is presented explicitly, never replaced by zero unless zero is confirmed", "owner_function": "calculate_material_balance", "landing": {"kind": "property", "expression": "result == material_planned - material_actual"}},
    {"id": "INV-050", "statement": "all derived values are deterministic and recomputable from owning records for the same accepted inputs", "owner_function": "build_project_overview", "landing": {"kind": "note", "text": "build_project_overview: [RETURN_SHAPE] MUST independently derive all five availability/value pairs from owning records and live project context; available values are populated, unavailable values are None, and failures MUST NOT fabricate zeros."}},
    {"id": "INV-051", "statement": "every action in the audit catalog writes exactly one AuditRecord with actor provenance; service actors record key_version", "owner_function": "append_audit", "landing": {"kind": "note", "text": "append_audit: [ORCHESTRATION] MUST validate the closed catalog intent and insert exactly one immutable AuditRecord into the caller's open transaction; service actors MUST retain key_version and no secret/file_ref may enter details."}},
    {"id": "INV-052", "statement": "audit records and import records are append-only; no flow mutates or deletes them", "owner_function": "PortalTransaction.commit", "landing": {"kind": "note", "text": "PortalTransaction.commit: [ORCHESTRATION] MUST atomically persist the complete write set or none, enforce at most one active viewer and producer grant per respective pair, preserve exact expense allocation sums and section reference restrictions, and forbid update/delete of AuditRecord, SnapshotImportRecord, and ApprovedEstimateSnapshot."}},
    {"id": "INV-053", "statement": "rejected significant mutations are audited with result=rejected and a closed reason_code", "owner_function": "append_audit", "landing": {"kind": "note", "text": "append_audit: [ORCHESTRATION] MUST validate the closed catalog intent and insert exactly one immutable AuditRecord into the caller's open transaction; service actors MUST retain key_version and no secret/file_ref may enter details."}},
    {"id": "INV-054", "statement": "list_active_projects returns the same typed Registry facts ordered by project_id ascending regardless of upstream response order", "owner_function": "list_active_projects", "landing": {"kind": "property", "expression": "all(str(result[i].project_id) <= str(result[i + 1].project_id) for i in range(len(result) - 1))"}},
    {"id": "INV-055", "statement": "list_viewer_projects returns accessible ProjectListItem values ordered by display_name.casefold() ascending without changing display_name and project_id ascending as the stable tie", "owner_function": "_ordered_project_items", "landing": {"kind": "note", "text": "_ordered_project_items: [DETERMINISM_OR_ORDERING] MUST return every item exactly once ordered by display_name.casefold() ascending without changing display_name, then project_id ascending as the stable tie."}}
  ]
}
