# State 7 trace repair — Flow 6 rule address

evaluate_vps_release: [RULE_REFERENCE] Durable release evidence must cover the complete resolved working set; use = rules.retention_release.required_working_set_coverage.
